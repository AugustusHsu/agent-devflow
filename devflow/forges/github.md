# forge: github

衍生值對照表，**只有通用節**（值欄宣稱可重用的工具能力或程序，repo 與環境只是可替換的參數）。本機節——具名的環境 instance——住 `devflow.local/<同路徑>`，不隨 kit 安裝。分節與狀態欄依 `R9`。

## 通用

| 面向 | 職位 | 值 | 狀態 |
|---|---|---|---|
| 審查載體 | — | Pull Request | ✅ 可用（實測 2026-09-11，驗證方式兩項全驗：(1) `gh api repos/AugustusHsu/agent-devflow --jq .has_issues` 期望 `true`，實際 `true`——issue 與 PR 是兩條並存的通道，審查載體不是靠關掉 issue 才落在 PR 上；(2) 審查結論確實落在 PR 上——`gh pr view 34 --json comments --jq '.comments[] \| select(.body \| test("^## 第[一二三四五六七八九十]+輪審查：`(APPROVE\|REQUEST_CHANGES)` @ `[0-9a-f]{7}`")) \| .url'` 期望至少一則，實際輸出五則（第一～第五輪，最後一則 `https://github.com/AugustusHsu/agent-devflow/pull/34#issuecomment-5624993819`，body 首行 `## 第五輪審查：`APPROVE` @ `51dce7d``，該 sha 等於 `gh pr view 34 --json headRefOid` 前 7 碼）——判定點是 comment **body** 含 verdict 與 head sha，只數 comments 數量會被純 CI／狀態留言誤判為通過；`reviews` 物件在本 repo 不承載結論（見「審查證據（`R3`／`R5`）」列）。第三者現在可照做。受測環境：2026-09-11；gh 2.97.0（`gh --version` 讀回 `gh version 2.97.0 (2026-07-31)`）；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），本格兩條指令實際只需 pull；目標 repo `AugustusHsu/agent-devflow`（公開、單人）；證據：上述兩條指令輸出） |
| CLI | — | `gh` | ✅ 可用（實測 2026-09-11，依 `R9` 自原「CLI」一格拆出：本格只宣稱「GitHub 的 CLI 是 `gh`」，本機安裝的版本與登入狀態是 instance 事實，見本機節「CLI（本機 instance）」格。驗證方式：`gh --version`，首行為 `gh version <版本> (<日期>)`、次行為 `https://github.com/cli/cli/releases/tag/v<版本>`——次行指向 GitHub 官方 `cli/cli` 的 release，即 `gh` 就是 GitHub CLI；本次實際輸出 `gh version 2.97.0 (2026-07-31)` 與 `https://github.com/cli/cli/releases/tag/v2.97.0`（該次執行同時是本機節該格的第 (1) 項）。第三者在任何裝有 `gh` 的環境現在可照做；讀到的版本號是環境參數，不是本格的宣稱。受測環境：2026-09-11；gh 2.97.0；執行身分為本機安裝 `gh` 的 OS 使用者（`gh --version` 不需任何 repo 權限）；目標 repo：不適用；證據：上述指令輸出） |
| 留言／label | implementer／coordinator／approver | `gh issue comment` ／ `gh issue edit --add-label`（增量，已有該 label 時不報錯） | ✅ 可用（實測 2026-09-11，依 `R7` 自原「建 issue／留言／label」一格拆出；`gh issue create` 本單未實測，見下一列。驗證方式兩項全驗，第三者可對任一自己有 write 權限的 issue 照做：(1) 留言——`gh issue comment 35 --body "R8 通道驗證 $(date -Iseconds)"` 期望 stdout 為新留言 URL，實際輸出 `https://github.com/AugustusHsu/agent-devflow/issues/35#issuecomment-5625048753`；再以該 URL 的 comment ID 讀回——`gh api repos/AugustusHsu/agent-devflow/issues/comments/5625048753 --jq '{url: .html_url, body}'`，實際 `{"body":"R8 通道驗證 2026-09-11T04:32:32+08:00","url":"https://github.com/AugustusHsu/agent-devflow/issues/35#issuecomment-5625048753"}`——URL 落在 issue #35 底下、body 與送出值逐字相符，排除「留言成功但落到別的 issue」。**不要用 `.comments[-1]`**：後續留言會使「最後一則」變動，那是 `R8` 禁止的一次性條件；重跑時以自己剛送出的 comment ID 讀回。(2) label 增量——issue #35 執行前已帶 `process`，`gh issue edit 35 --add-label process` 期望 exit 0 且不報錯（冪等），實際 stdout `https://github.com/AugustusHsu/agent-devflow/issues/35`、無錯誤；`gh issue view 35 --json labels --jq '.labels[].name'` 期望仍只列 `process`（不重複、不新增），實際輸出 `process`。受測環境：2026-09-11；gh 2.97.0；`date` 為 GNU coreutils 9.4；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），兩項實際只需 push／write；目標 repo `AugustusHsu/agent-devflow`；證據：issue #35 留言 https://github.com/AugustusHsu/agent-devflow/issues/35#issuecomment-5625048753） |
| 建 issue | coordinator | `gh issue create` | ✅ 可用（實測 2026-09-21，在可丟棄的消費者 repo 上現場跑，原格所列的升級條件兩項全驗。驗證方式，第三者對任一自己有 write 權限的 repo 照做：(1) 把 issue 本文寫成檔案 `<body.md>`（依 `templates/issue.md` 填好），執行 `gh issue create --title "<title>" --body-file <body.md>`，記下 stdout 的 issue URL 與號碼 `<N>`；(2) 讀回——`gh issue view <N> --json title --jq .title` 期望與送出的 `<title>` 逐字相同；`gh issue view <N> --json body --jq .body` 與 `<body.md>` 比對（兩邊都把 `\r\n` 正規化為 `\n` 並去尾端換行後）期望逐 byte 相同。實跑：消費者 repo `AugustusHsu/devflow-e2e` issue #5——送出指令、讀回指令與結果（title 逐字相同；body 正規化後 1295 bytes 逐 byte 相同）全文記在該 issue 的「派工紀錄（`L1`）」留言首段 https://github.com/AugustusHsu/devflow-e2e/issues/5#issuecomment-5754725217 ，第三者可對著留言重跑同兩條讀回指令。受測環境：2026-09-21，`gh version 2.97.0 (2026-07-31)`，執行身分為本機使用者以 `gh auth login` 的 token（scope 含 `repo`），目標 repo 為私有 `AugustusHsu/devflow-e2e`。證據：https://github.com/AugustusHsu/devflow-e2e/issues/5（本文＋ `L1` 留言）、https://github.com/AugustusHsu/agent-devflow/issues/142（E7 段）） |
| 從 issue 開分支 | coordinator | `gh issue develop <N> --base <分支> --name <N>-<slug>`（本 repo 派工時 `<分支>` 為整合分支 `main`），涵蓋三項能力，依 `R7` 逐項標明，三項皆**實測**：(a) 在 origin 直接建遠端分支，指向 `--base` 所指分支**當時的 tip**（以非預設分支當 `--base` 驗過：sha 等於該分支、不等於預設分支 `main`），並 fetch 為本機 tracking ref（stdout 末行 ` * [新分支]          <N>-<slug> -> origin/<N>-<slug>`；`[新分支]` 是 git 在 `LANG=zh_TW.UTF-8` 下的字樣，比對 `-> origin/<N>-<slug>` 片段即可），**不**建本機分支、**不** checkout（無 `-c`）；本格的驗證程序假設 origin 上尚無同名分支，撞名時的行為未測；(b) 分支與 issue 連結（issue 側欄 Development）：開 PR 前 `gh issue develop <N> --list` 讀回一列 `<N>-<slug>\t<tree URL>`；由該分支**對預設分支**開 PR 後（本文不含任何關鍵字、`#`、issue 號），`--list` 回空、GraphQL `linkedBranches` 為空（分支仍在也一樣），該 PR 的 `closingIssuesReferences` 出現 `<N>`（PR #16 曾觀察到剛建立時立即讀為 `[]`、稍後重讀為 `[15]`；PR #10 則立即讀即為 `[9]`——讀回可能有短暫延遲，延遲上界未測）；對**非預設分支**開 PR 則以上皆不變：`--list` 仍一列、`closingIssuesReferences` 空；(c) 由此分支對預設分支開的 PR（本文同上、無任何參照）**合併進預設分支**後，該 issue 隨即 `CLOSED`／`stateReason: COMPLETED`（timeline 的 `closed` 事件 `actor` 記為合併者、`commit_id` 與 `state_reason` 皆 `null`，事件不記原因）；同樣的 PR 合併進**非預設分支**不關閉；對照組——**不經** `gh issue develop` 的分支對預設分支開 PR（本文同樣無參照）並合併，issue 仍 `OPEN`。AC 未完就合併的 issue 因此要人工重開 | ✅ 可用（實測 2026-09-21，升自 `⬜ 未測`。值欄依 `R7` 改寫：原只列 `gh issue develop <N> --base main`、分支名寫成旁述，實際生效的是 `--name`；原值欄未載 (b)(c) 的 PR 連動與關閉行為，第三者照原宣稱做會踩到。驗證方式，第三者在自己有 admin 權限的可丟棄 repo 上照做（可重跑任意次，每次都是新事件）——前提：對該 repo 能建 issue、push、建 PR，且預設分支 `D`（`gh api repos/<o>/<r> --jq .default_branch` 讀回）沒有未滿足的保護規則／required check／merge queue，且 repo 允許 merge commit（`gh api repos/<o>/<r> --jq .allow_merge_commit` 期望 `true`；步驟 (6) 用 `--merge`，其他合併方式是否同樣觸發 (c) 未測），否則步驟 (6) 的合併會被擋，那是 repo 設定、不是本格能力的反例。實驗一，驗 (a)：(1) 建一條**非預設**分支 `B` 推上 origin，`gh issue create` 開 issue `N`；(2) `gh issue develop N --base B --name N-x`，期望 exit 0、stdout 含 `-> origin/N-x`，且 `git branch --list N-x` 為空、`git rev-parse --abbrev-ref HEAD` 不變；(3) `git ls-remote --heads origin N-x` 的 sha 期望 ＝ `git rev-parse origin/B` 且 ≠ `git rev-parse origin/D`——用非預設分支才分得出「`--base` 生效」與「剛好落在預設分支」。實驗二，驗 (b)(c)：(4) 另開 issue `P`，`gh issue develop P --base D --name P-x`，`gh issue develop P --list` 期望一列；(5) 在 `P-x` 推一個 commit，`gh pr create --base D --head P-x --title t --body t`（`t` 不含關鍵字、`#`、任何 issue 號），再跑 `--list` 期望**回空**，`gh pr view <PR> --json closingIssuesReferences` 期望含 `P`（讀回可能有短暫延遲：立即讀為 `[]` 時重讀，重讀仍為 `[]` 則本次驗證不通過、不得進步驟 (6)）；(6) `gh pr merge <PR> --merge` 後 `gh issue view P --json state,stateReason` 期望 `CLOSED`／`COMPLETED`。對照組：issue `M` ＋ 一條**不經** `gh issue develop` 建的分支，對 `D` 開 PR（本文同上）並合併，`gh issue view M --json state` 期望仍 `OPEN`。**`--list` 回空不代表分支被刪**：分支已刪、或已由該分支對預設分支開 PR，都回空（本 repo 實例：#155 的 `155-tables-checkout` 仍在 origin，因 PR #158 已開而 `--list` 回空），所以 (4) 的讀回必須在開 PR 前跑，且不得拿別人已開 PR 或已刪分支的 issue 當讀回。能區分 (c) 與「別的原因關閉」的是對照組與非預設分支組，不是合併與關閉的時間同刻。實跑（可丟棄 repo `AugustusHsu/devflow-e2e`：私有、預設分支 `main`、`main` 無保護（`protected:false`）、`allow_merge_commit:true`、`deleteBranchOnMerge=false`）——實驗一：issue #8 ＋ 非預設分支 `probe-base`（tip `7224d10`）：`gh issue develop 8 --base probe-base --name 8-on-probe-base` → ` * [新分支]          8-on-probe-base -> origin/8-on-probe-base`，`ls-remote` → `7224d10ec8f367697f92d635155b5b81295913a2`（＝ `probe-base`、≠ `main` `c8de051`），`git branch --list 8-on-probe-base` 空、HEAD 仍 `main`，`--list` → `8-on-probe-base\thttps://github.com/AugustusHsu/devflow-e2e/tree/8-on-probe-base`。實驗二：issue #15 ＋ `f-15`（`--base main`）：開 PR 前 `--list` 一列、`linkedBranches=[f-15]`；PR #16（title／body 皆 `probe f`）建立後 `--list` 空、`linkedBranches=[]`，`closingIssuesReferences` 立即讀 `[]`、稍後重讀 `[15]`（一次觀察）；合併（`2026-09-21T11:55:09Z`，merge commit `2e0de50`，`baseRefName: main`）後 issue #15 → `CLOSED COMPLETED 2026-09-21T11:55:10Z`。非預設分支組：issue #13 ＋ `e-13`（`--base probe-base-2`）、PR #14（`--base probe-base-2`，body `probe e`）：開 PR 後 `--list` 仍一列、`linkedBranches=[e-13]`、`closingIssuesReferences=[]`；合併（`11:54:36Z`）後 issue #13 仍 `OPEN`。對照組：issue #17 ＋ 未連結分支 `g-17`、PR #18（`--base main`，body `probe g`，`closingIssuesReferences=[]`）合併（`11:55:30Z`）後 issue #17 仍 `OPEN`；#13／#17 事後人工關閉。首次觀察在本 repo：#147 的 `gh issue develop 147 --base main --name 147-rules-v7-v3-l1`（https://github.com/AugustusHsu/agent-devflow/issues/147#issuecomment-5756763791 ，含 stdout 原文與 `--list`／`ls-remote` 讀回）。受測環境（`R10`）：首次驗證與最近確認同為 2026-09-21；gh 2.97.0（`gh version 2.97.0 (2026-07-31)`）；`LANG=zh_TW.UTF-8`；執行身分 AugustusHsu ＝ 兩個 repo 的 owner（`permissions.admin: true`）——(a)(b) 需 write／push，(c) 的合併另受目標預設分支的保護規則約束：devflow-e2e 的 `main` 無保護故可直合；本 repo `main` 受保護（required check `devflow-checks`）——約束 (c) 的合併步驟，不約束 (a)(b)。證據：devflow-e2e issue #8／#13／#15／#17 與 PR #14／#16／#18（皆保留）、上述 #147 留言） |
| tag 保護 | — | repo ruleset（`gh api -X POST repos/<o>/<r>/rulesets --input <ruleset.json>`）：`target: tag`、`enforcement: active`、`conditions.ref_name.include: ["refs/tags/v*"]`、`rules` 取 `update` ＋ `deletion`、`bypass_actors: []`——`v*` tag 不能被移動也不能被刪除，admin 亦不可繞，這是 `V5`（已發版本不移動、不刪、不重打）在 GitHub 端的機械保證。限制（**未測**）：本 ruleset 未設 `creation`，建 tag 的權限等同 push 權限；「`creation` 規則能否搭 `bypass_actors` 的 `RepositoryRole` 只放行特定角色建立」本次**未驗證**（要驗得 POST 一條帶角色 bypass 的 `creation` 規則並以非該角色的身分推 tag，本 repo 只有一個協作者，做不到後半），不設 `creation` 的實際理由是後者 | ✅ 可用（實測 2026-09-22，升自 `⬜ 未測`，值欄依 `R7` 自原「rulesets；無法依角色限制建立者」改寫成實際生效的機制與參數。三項宣稱逐項皆驗：`deletion`、`update`、`bypass_actors: []`（以 admin 身分跑仍被擋）；「依角色限制建立者」一項未測，值欄已標。驗證方式，第三者在自己具 admin 的 repo 上照做，以該 repo 一個既有的 `v*` tag 為參數：(1) 以上述 POST 建同形 ruleset，`gh api repos/<o>/<r>/rulesets --jq '.[] \| {id, name, target, enforcement}'` 讀回 `target: tag`、`enforcement: active`；(2) 刪既有 tag——`git push origin :refs/tags/<既有 tag>` 期望 exit 1，stderr 含 `remote: error: GH013: Repository rule violations found` 與 `Cannot delete this tag`；(3) force 移動——`git push --force origin <另一個 commit>:refs/tags/<既有 tag>` 期望 exit 1 且含 `GH013` 與 `Cannot update this protected ref`，再讀回該 tag 的 sha 不變；(4) 建**新** tag 仍可——`git push origin refs/tags/<新 tag>` 期望 `[new tag]`、exit 0，且對這個新 tag 重跑 (2) 一樣被擋（規則不分新舊，這條把「只是舊 tag 剛好受保護」排除掉）。清理 probe tag 必須先 `PUT …/rulesets/<id>` 設 `enforcement=disabled`，刪完再設回 `active`。本 repo 現在可重跑的一條：`git push origin :refs/tags/v0.0.0.1`，期望 `GH013 … Cannot delete this tag`、exit 1——它被拒且 tag 不動，但仍是對真 repo 的寫嘗試，重跑前先確認跑在哪個 repo。受測環境（`R10`）：首次驗證與最近確認同為 2026-09-22；gh 2.97.0；執行身分 AugustusHsu，對目標 repo 具 admin（(1) 與清理需 admin，(2)(3)(4) 需 push）；目標 repo 為公開的 `AugustusHsu/agent-devflow`，ruleset id `23780350`、名稱「kit release tags (V5)」，設定後遠端 rulesets 恰一條。副作用已確認：probe tag 觸發 `docs.yml` run 35630631769，在「決定版本」步驟以 `💥 tag 不是 v<a.b.c.d>` exit 2（設計如此），`versions.json` 未變。證據：issue #166 留言 https://github.com/AugustusHsu/agent-devflow/issues/166#issuecomment-5764535632 ，含四個案例的指令與輸出原文、`v0.0.0.1`／`v0.1.1.0` 設定前後的 sha 讀回，與 probe tag 的建立及清理紀錄） |
| 開 PR | implementer／coordinator（代行） | `gh pr create --base main --body-file <templates/pr.md 填好>` | ✅ 可用（實測 2026-09-11，驗證方式兩項全驗，第三者可對任一自己有 write 權限的 repo 照做：在 worktree 內執行 `gh pr create --base main --title "<title>" --body-file <由 templates/pr.md 填好的檔案>`，記下 stdout 的 PR URL 與號碼 `<N>`；再 `gh pr view <N> --json baseRefName --jq .baseRefName` 期望 `main`——證明 `--base main` 生效，而非落到 repo 預設 base；`gh pr view <N> --json body,headRefOid --jq '. as $r \| {secs: [$r.body \| scan("## (基準\|對應 AC\|驗證\|Write scope 自查)") \| .[0]], head_ok: ($r.body \| test("head：`" + $r.headRefOid[0:7] + "`"))}'` 期望 `secs` 含 `templates/pr.md` 的四個必要節、`head_ok` 為 `true`——證明 body 來自填好的模板且 head sha 與實際 head 一致（`L4`），而非被 commit 訊息或 `--fill` 取代。受測環境：2026-09-11；gh 2.97.0；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），開 PR 實際只需 push／write；目標 repo `AugustusHsu/agent-devflow`；另以 `gh api graphql -f query='{ repository(owner:"<o>", name:"<r>") { pullRequest(number:<N>) { lastEditedAt } } }' --jq .data.repository.pullRequest.lastEditedAt` 期望 `null`——證明 body 是 create 當下所給、從未 edit（**不要用 `createdAt` = `updatedAt`**：close、刪分支等事件都會更新 `updatedAt`，關閉後即不成立）；`diff <(gh pr view <N> --json body --jq .body) <body-file>` 期望僅檔尾換行差（body-file 須保留供比對）。證據：`https://github.com/AugustusHsu/agent-devflow/pull/37`（驗證專用 draft PR，空 commit `ff4428f`，驗完即關不合併）——orchestrator 以填好的模板檔 `gh pr create --draft --base main --body-file` 建立後**未經任何 edit** 即讀回：`baseRefName` = `main`，`secs` = `["基準","對應 AC","驗證","Write scope 自查"]`，`head_ok` = `true`，`lastEditedAt` = `null`（PR 關閉後讀回仍為 `null`；對照 PR #36 曾 edit，讀回 `2026-09-10T21:06:45Z`），body diff 僅第 25 行檔尾空行；比對用的 body-file 未保留在 repo，第三者重跑時自備填好的模板檔並保留至比對完成。取證用 PR 會永久留在 `gh pr list --state all`，不影響 `--state merged` 與 main；日後同類取證改在可丟棄 repo 進行） |
| 審查證據（`R3`／`R5`） | coordinator | `gh pr comment <N> --body "<審查結論；body 內寫明 head sha>"`；`gh pr review --approve\|--request-changes` 在單人 repo 不可用（本檔「已知限制」第一條） | ✅ 可用（實測 2026-09-11，值欄依 `R7` 改寫：原列 `gh pr review <N> --approve\\|--request-changes --body` 與「review 原生綁 commit」，但同一帳號不能核可自己的 PR，單人 repo 走不到該指令；實際生效的機制是 `gh pr comment` 寫審查結論、body 寫明 head sha 以補回 review 原生的 commit 綁定（接 `R3`）。驗證方式（與「合併」列同口徑：讀回第三者現在仍可查的持久成品）：`gh pr view 34 --json comments,headRefOid --jq '{head: .headRefOid[0:7], last: (.comments \| map(select(.body \| test("^## 第.+輪審查："))) \| last \| {url, first: (.body \| split("\n")[0])})}'` 期望 `last.first` 含 `APPROVE` 或 `REQUEST_CHANGES` 且含 `head` 的 7 碼，實際輸出 `{"head":"51dce7d","last":{"url":"https://github.com/AugustusHsu/agent-devflow/pull/34#issuecomment-5624993819","first":"## 第五輪審查：`APPROVE` @ `51dce7d`"}}`。寫入動作本身：對任一有 write 權限的 PR 執行 `gh pr comment <PR-N> --body "<結論> @ <head sha>"`，記下 stdout 的 comment URL，以固定 ID 讀回 `gh api repos/<o>/<r>/issues/comments/<ID> --jq '{url: .html_url, first: (.body \| split("\n")[0])}'` 期望 `first` 含結論與 `gh pr view <PR-N> --json headRefOid` 前 7 碼。orchestrator 實跑於 PR #37：stdout `https://github.com/AugustusHsu/agent-devflow/pull/37#issuecomment-5625433360`，讀回 `first` = `REQUEST_CHANGES @ `ff4428f`（R8 通道驗證…）`，`headRefOid[0:7]` = `ff4428f`。受測環境：2026-09-11；gh 2.97.0；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入，讀回只需 pull、寫入需 write；目標 repo `AugustusHsu/agent-devflow`（公開、單人）；證據：寫入 `https://github.com/AugustusHsu/agent-devflow/pull/37#issuecomment-5625433360`（驗證用，PR #37 已關）；真實審查留言例 PR #34 `https://github.com/AugustusHsu/agent-devflow/pull/34#issuecomment-5624993819`） |
| 合併（`I3`） | approver／coordinator（`M2`） | `gh pr merge <PR-N> --merge --subject "<gitmoji> merge(#<issue-N>): …" --body "PR #<PR-N>; reviewer …; head <sha>"` | ✅ 可用（實測 2026-09-11，依 `R9` 自原「合併（`I3`）」一格拆出：本格只宣稱合併指令與其三個選項的效果，原值欄的「repo 層：`allow_squash_merge=false`、`allow_rebase_merge=false`、`delete_branch_on_merge=true`」是本 repo 的設定，見本機節「repo 層合併設定」格。值欄依 `R7` 改寫一處：原寫死 `--subject "🔀 merge(#N): …"`，但讀回 PR #34 的 merge commit subject 實為 `📝 merge(#33): …`（`git log --oneline --merges -8 origin/main`：#2／#7／#14／#18／#28 用 🔀，#4／#30／#33 用該次變更本身的 gitmoji），實際生效的機制是 `--subject` 原樣接受任意字串、gitmoji 並非固定值，故改為 `<gitmoji>`。驗證方式兩項全驗（原第 (2)(3) 項；原第 (1) 項是 repo 設定讀回，隨該事實移到本機節）：(2) `--merge` 確實產生兩親 merge commit——`gh pr view 34 --json mergeCommit --jq .mergeCommit.oid` 得 `8029dfa12092701c3a6807405e2b047d036b1745`，`git cat-file -p 8029dfa12092701c3a6807405e2b047d036b1745 \| grep -n '^parent'` 期望兩行，實際 `2:parent 333d087c9f529cddcf04d00db587c7a52c50cea8` 與 `3:parent 51dce7d6ec33d7b3d38240925342dcddda9024d8`，第二個 parent 等於 `gh pr view 34 --json headRefOid --jq .headRefOid` 的 `51dce7d6ec33d7b3d38240925342dcddda9024d8`（逐行定位，不以計數為唯一依據）；(3) `--subject`／`--body` 生效——`git log -1 --format='%s%n%b' 8029dfa12092701c3a6807405e2b047d036b1745` 實際輸出 subject `📝 merge(#33): hermes.md 派工／worktree／中斷交接三格結帳，HITL 降 ⬜`、body 首行 `PR #34; reviewer codex(gpt-5.6-sol) ×5; head 51dce7d`，三段與值欄 body 的 `PR #N; reviewer …; head <sha>` 逐段對應；此處讀的是該兩個選項**產出的成品**，不是拿 commit 訊息的自述當證據。`gh pr merge` 無法對已合併 PR 重跑，故依 `R8` 採讀回成品的前瞻讀法——(2)(3) 對本 repo 任一已合併 PR 皆可照做，第三者現在可重現。受測環境：2026-09-11；gh 2.97.0；`grep` 為 GNU grep 3.11；git 2.43.0（orchestrator 於主 checkout 實測 `git version 2.43.0`；coder 白名單無 `git --version`）；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），(2)(3) 只需 pull ＋ 本機有該 repo 的 clone；目標 repo `AugustusHsu/agent-devflow`；證據：上述指令輸出 ＋ merge commit `8029dfa12092701c3a6807405e2b047d036b1745`） |
| 已合併訊號（`C1`） | coordinator | `gh pr view <N> --json state,mergedAt,mergeCommit` ＋ `git merge-base --is-ancestor` | ✅ 可用（實測 2026-09-11，驗證方式兩項全驗：(1) forge 端——`gh pr view 34 --json state,mergedAt,mergeCommit --jq '{state, mergedAt, mergeCommit: .mergeCommit.oid}'` 期望 `state` 為 `MERGED`、`mergedAt` 非 null、`mergeCommit` 為 sha，實際輸出 `{"mergeCommit":"8029dfa12092701c3a6807405e2b047d036b1745","mergedAt":"2026-09-10T20:27:56Z","state":"MERGED"}`；(2) 本地 git 端獨立確認——`gh pr view 34 --json headRefOid --jq .headRefOid` 取 head `51dce7d6ec33d7b3d38240925342dcddda9024d8`，執行 `git merge-base --is-ancestor 51dce7d6ec33d7b3d38240925342dcddda9024d8 origin/main` 期望 exit 0，實際 exit 0；同一次以 `git rev-parse origin/main` 讀回 `8029dfa12092701c3a6807405e2b047d036b1745`，確認比對的是當下 main。兩項互補且缺一不可：(1) 只是 forge 自述，反例「PR 標 `MERGED` 但 head 不在 main 的歷史裡」要靠 (2) 擋下。第三者現在可對本 repo 任一已合併 PR 照做（本機需先有 `origin/main` 的 fetch）。受測環境：2026-09-11；gh 2.97.0；git 2.43.0（orchestrator 於主 checkout 實測 `git version 2.43.0`；coder 白名單無 `git --version`）；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），兩項實際只需 pull；目標 repo `AugustusHsu/agent-devflow`；證據：上述指令輸出） |
| 合併後刪分支（`C1`） | coordinator | repo 設定 `delete_branch_on_merge=true` 開啟時，合併由 GitHub 自動刪遠端分支；`git ls-remote --heads origin <branch>` 驗 | ✅ 可用（實測 2026-09-11，依 `R9` 自原「合併後刪分支（`C1`）」一格拆出：本格只宣稱 GitHub 的自動刪分支機制與讀回方式，「本 repo 的 `delete_branch_on_merge` 為 `true`」是本 repo 的設定，其讀回（原第 (1) 項）併入本機節「repo 層合併設定」格。值欄依 `R7` 改寫：原列 `gh pr merge --delete-branch` 或 `git push origin --delete <branch>` 兩法皆未實測，本 repo 今日的合併都沒有帶 `--delete-branch`，實際生效的是 repo 設定 `delete_branch_on_merge=true`，故改寫後只留該機制、不沿用原宣稱。驗證方式：(2) 機制生效——`gh pr view 34 --json headRefName --jq .headRefName` 取已合併 PR 的分支名 `33-hermes-cells`，`git ls-remote --heads origin 33-hermes-cells` 期望無輸出，實際無輸出；為排除「遠端連不上時同樣是空輸出」這個反例，同一次另跑 `git ls-remote --heads origin`，實際只輸出兩行——`b791d4d408c8ae7a3dd1e8ba4b896a62b0d8d94b` → `refs/heads/4-lookup-table-settlement`、`8029dfa12092701c3a6807405e2b047d036b1745` → `refs/heads/main`，證明遠端可連通且 `33-hermes-cells` 確實不在其中。第三者現在可對本 repo 任一已合併 PR 照做；換 repo 時以該 repo 的已合併 PR 為參數，且該 repo 的 `delete_branch_on_merge` 須先為 `true`（本 repo 的讀值見本機節「repo 層合併設定」格）。受測環境：2026-09-11；gh 2.97.0；git 2.43.0（orchestrator 於主 checkout 實測 `git version 2.43.0`；coder 白名單無 `git --version`）；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），(2) 只需 pull；目標 repo `AugustusHsu/agent-devflow`；證據：上述指令輸出） |
| CI 位置 | — | `.github/workflows/` | ✅ 可用（實測 2026-09-11，驗證方式兩項全驗：(1) 檔案位置——在該 repo 的工作目錄執行 `ls .github/workflows/` 期望列出 workflow 檔，實際輸出 `devflow-checks.yml`；(2) 該位置確實被 GitHub 讀取並執行——`gh run list --workflow=devflow-checks.yml --limit 1` 期望至少一筆 run，實際輸出一列（tab 分隔）`completed`、`success`、`📝 process(對照表): hermes.md 派工／worktree／HITL／中斷交接四格結帳`、`devflow checks`、`33-hermes-cells`、`pull_request`、`34526392736`、`11s`、`2026-09-10T20:26:31Z`。(1) 單獨只證「檔案在那個路徑」，反例「檔案放對但 GitHub 沒在跑」要靠 (2) 擋下。第三者現在可照做。受測環境：2026-09-11；gh 2.97.0；`ls` 為 GNU coreutils 9.4；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），(1) 需本機有該 repo 的 checkout、(2) 只需 pull；目標 repo `AugustusHsu/agent-devflow`；證據：上述兩條指令輸出 ＋ run `34526392736`） |
| 人類站（`D3`） | — | GitHub Pages 供應 `gh-pages` 分支根目錄；分支由 `.github/workflows/docs.yml`（checkout `ref: main` 取工具鏈、內容取指定 tag）以 mike 寫入，觸發方式 `gh workflow run docs.yml --ref main -f tag=<tag>`（對既有 tag 建版；`latest` 只在該 tag 為已建版本中最高時才掛）。Pages 本身用 `gh api -X POST repos/<owner>/<repo>/pages -f 'source[branch]=gh-pages' -f 'source[path]=/'` 啟用一次 | ✅ 可用（實測 2026-09-21，本 repo（公開）首次建站；工具版本 mkdocs 1.6.1、mike 2.2.0（CI log）、gh 2.97.0；執行身分 repo owner／admin——`gh workflow run` 需 write 權限，`POST …/pages` 需 admin、maintainer 或具「manage GitHub Pages settings」的自訂角色。驗證方式，第三者對站工具鏈已在 main 的 repo 照做，以 `<tag>` 為參數（本次 `v0.0.0.1`）：(1) `gh workflow run docs.yml --ref main -f tag=<tag>`，等 run 結束 `conclusion` 為 `success`，log 含 `本 tag <tag> 是最高版本：建版並掛 latest`（若 gh-pages 已有更高版本，workflow 會走 `已有更高版本 …：只建版 <tag>，latest 不動` 分支、(4) 的 `latest` 指向該更高版本——此分支未在 CI 出現過，只在本機 mike 模擬）；(2) `gh api repos/<owner>/<repo>/contents/versions.json?ref=gh-pages --jq .content \| base64 -d` 得 JSON 陣列，含 `"version": "<tag>"` 一項，最高版本項的 `aliases` 陣列含 `"latest"`；(3) 上述 POST 啟用 Pages 後約 1 分鐘 `gh api repos/<owner>/<repo>/pages --jq .status` 得 `built`；(4) `curl -s https://<owner>.github.io/<repo>/versions.json` 與 (2) 相同，`curl -s https://<owner>.github.io/<repo>/` 含 `url=latest/`、`/latest/` 含 `url=../<最高版本>/`。證據：#144 第 7 項留言（run 35564455924、首次 gh-pages tip `0bd73a9`——每次建版都會推新 commit，此值只證明當次）。反例未遇：本次只有一個版本、一次 run。值欄依 `R7` 只列已實測的 dispatch 觸發；docs.yml 另寫了 push 四碼 tag `v<a.b.c.d>` 自動建版與非四碼 tag 由 step 以 exit 2 拒絕，以及回填舊 tag 不動 `latest` 的分支，三支都未在 CI 實跑（後者在本機 mike 模擬過：#144 R2 修正留言 issuecomment-5755579855 的四情境 `versions.json` 讀回，與 PR #145 第 4 則 review 5263197042 的 bare remote 實跑），首次 push tag 後再驗、屆時決定是否入值欄。限制：Pages 是公開站；私有 repo 需 GitHub Pro／Team／Enterprise 方可用（GitHub Free 必須是公開 repo），本 repo 為公開，未測私有路徑） |
| 故障退路（`F3`） | coordinator | `F3`（forge 回錯或逾時）在 GitHub 的讀回程序：尚未推導 | ⬜ 未測（值欄依 `R9` 自「—」改寫，只補上宣稱對象，未推導內容） |

## 已知限制（文件推導，待實測）

- 同一帳號不能核可自己的 PR；單人 repo 的 required approvals 必須是 0，審查證據靠 review 內容與 head sha，不靠 approval 計數。
- REST `/issues` 會混入 PR，列 issue 時要過濾。
- 公開 repo 預設任何人可送 review；必要時開 code review limits。
