# forge: github

衍生值對照表。分節與狀態欄依 `R9`：**通用**節的值欄宣稱可重用的工具能力或程序（repo 與環境只是可替換的參數），**本機**節的值欄宣稱具名的環境 instance（本機安裝的工具、帳號或組織、本 repo 的設定與資源）；狀態取 `✅`／`📝`／`⬜` 三值之一，兩節的 `✅` 判準各依 `R9` 定義，非 `✅` 不得在流程中當作可用。「職位」欄記該格是哪個職位的用法（詞彙見 `seats/`），`—` 表示是工具或資源本身的性質，不專屬任一職位。

## 通用

| 面向 | 職位 | 值 | 狀態 |
|---|---|---|---|
| 審查載體 | — | Pull Request | ✅ 實測 2026-09-11（驗證方式兩項全驗：(1) `gh api repos/AugustusHsu/agent-devflow --jq .has_issues` 期望 `true`，實際 `true`——issue 與 PR 是兩條並存的通道，審查載體不是靠關掉 issue 才落在 PR 上；(2) 審查結論確實落在 PR 上——`gh pr view 34 --json comments --jq '.comments[] \| select(.body \| test("^## 第[一二三四五六七八九十]+輪審查：`(APPROVE\|REQUEST_CHANGES)` @ `[0-9a-f]{7}`")) \| .url'` 期望至少一則，實際輸出五則（第一～第五輪，最後一則 `https://github.com/AugustusHsu/agent-devflow/pull/34#issuecomment-5624993819`，body 首行 `## 第五輪審查：`APPROVE` @ `51dce7d``，該 sha 等於 `gh pr view 34 --json headRefOid` 前 7 碼）——判定點是 comment **body** 含 verdict 與 head sha，只數 comments 數量會被純 CI／狀態留言誤判為通過；`reviews` 物件在本 repo 不承載結論（見「審查證據（`R3`／`R5`）」列）。第三者現在可照做。受測環境：2026-09-11；gh 2.97.0（`gh --version` 讀回 `gh version 2.97.0 (2026-07-31)`）；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），本格兩條指令實際只需 pull；目標 repo `AugustusHsu/agent-devflow`（公開、單人）；證據：上述兩條指令輸出） |
| CLI | — | `gh` | ✅ 實測 2026-09-11（依 `R9` 自原「CLI」一格拆出：本格只宣稱「GitHub 的 CLI 是 `gh`」，本機安裝的版本與登入狀態是 instance 事實，見本機節「CLI（本機 instance）」格。驗證方式：`gh --version`，首行為 `gh version <版本> (<日期>)`、次行為 `https://github.com/cli/cli/releases/tag/v<版本>`——次行指向 GitHub 官方 `cli/cli` 的 release，即 `gh` 就是 GitHub CLI；本次實際輸出 `gh version 2.97.0 (2026-07-31)` 與 `https://github.com/cli/cli/releases/tag/v2.97.0`（該次執行同時是本機節該格的第 (1) 項）。第三者在任何裝有 `gh` 的環境現在可照做；讀到的版本號是環境參數，不是本格的宣稱。受測環境：2026-09-11；gh 2.97.0；執行身分為本機安裝 `gh` 的 OS 使用者（`gh --version` 不需任何 repo 權限）；目標 repo：不適用；證據：上述指令輸出） |
| 留言／label | implementer／coordinator／approver | `gh issue comment` ／ `gh issue edit --add-label`（增量，已有該 label 時不報錯） | ✅ 實測 2026-09-11（依 `R7` 自原「建 issue／留言／label」一格拆出；`gh issue create` 本單未實測，見下一列。驗證方式兩項全驗，第三者可對任一自己有 write 權限的 issue 照做：(1) 留言——`gh issue comment 35 --body "R8 通道驗證 $(date -Iseconds)"` 期望 stdout 為新留言 URL，實際輸出 `https://github.com/AugustusHsu/agent-devflow/issues/35#issuecomment-5625048753`；再以該 URL 的 comment ID 讀回——`gh api repos/AugustusHsu/agent-devflow/issues/comments/5625048753 --jq '{url: .html_url, body}'`，實際 `{"body":"R8 通道驗證 2026-09-11T04:32:32+08:00","url":"https://github.com/AugustusHsu/agent-devflow/issues/35#issuecomment-5625048753"}`——URL 落在 issue #35 底下、body 與送出值逐字相符，排除「留言成功但落到別的 issue」。**不要用 `.comments[-1]`**：後續留言會使「最後一則」變動，那是 `R8` 禁止的一次性條件；重跑時以自己剛送出的 comment ID 讀回。(2) label 增量——issue #35 執行前已帶 `process`，`gh issue edit 35 --add-label process` 期望 exit 0 且不報錯（冪等），實際 stdout `https://github.com/AugustusHsu/agent-devflow/issues/35`、無錯誤；`gh issue view 35 --json labels --jq '.labels[].name'` 期望仍只列 `process`（不重複、不新增），實際輸出 `process`。受測環境：2026-09-11；gh 2.97.0；`date` 為 GNU coreutils 9.4；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），兩項實際只需 push／write；目標 repo `AugustusHsu/agent-devflow`；證據：issue #35 留言 https://github.com/AugustusHsu/agent-devflow/issues/35#issuecomment-5625048753） |
| 建 issue | coordinator | `gh issue create` | ⬜ 未實測（依 `R7` 自原「建 issue／留言／label」一格拆出。本單刻意不跑——在目標 repo 會留下無法清除的垃圾 issue。issue #35 本身由 orchestrator 以 `gh issue create` 建立，但那是當時那次執行的紀錄，不是第三者現在能重跑的驗證方式，依 `R8` 不構成 `✅`。要升 `✅` 須在可丟棄的 repo 上現場跑一次，並以 `gh issue view <N>` 讀回 title／body 與送出值相符） |
| 從 issue 開分支 | coordinator | `gh issue develop <N> --base main`，分支名 `<N>-<slug>` | ⬜ 未實測 |
| tag 保護 | — | rulesets；無法依角色限制建立者 | ⬜ 未實測 |
| 開 PR | implementer／coordinator（代行） | `gh pr create --base main --body-file <templates/pr.md 填好>` | ✅ 實測 2026-09-11（驗證方式兩項全驗，第三者可對任一自己有 write 權限的 repo 照做：在 worktree 內執行 `gh pr create --base main --title "<title>" --body-file <由 templates/pr.md 填好的檔案>`，記下 stdout 的 PR URL 與號碼 `<N>`；再 `gh pr view <N> --json baseRefName --jq .baseRefName` 期望 `main`——證明 `--base main` 生效，而非落到 repo 預設 base；`gh pr view <N> --json body,headRefOid --jq '. as $r \| {secs: [$r.body \| scan("## (基準\|對應 AC\|驗證\|Write scope 自查)") \| .[0]], head_ok: ($r.body \| test("head：`" + $r.headRefOid[0:7] + "`"))}'` 期望 `secs` 含 `templates/pr.md` 的四個必要節、`head_ok` 為 `true`——證明 body 來自填好的模板且 head sha 與實際 head 一致（`L4`），而非被 commit 訊息或 `--fill` 取代。受測環境：2026-09-11；gh 2.97.0；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），開 PR 實際只需 push／write；目標 repo `AugustusHsu/agent-devflow`；另以 `gh api graphql -f query='{ repository(owner:"<o>", name:"<r>") { pullRequest(number:<N>) { lastEditedAt } } }' --jq .data.repository.pullRequest.lastEditedAt` 期望 `null`——證明 body 是 create 當下所給、從未 edit（**不要用 `createdAt` = `updatedAt`**：close、刪分支等事件都會更新 `updatedAt`，關閉後即不成立）；`diff <(gh pr view <N> --json body --jq .body) <body-file>` 期望僅檔尾換行差（body-file 須保留供比對）。證據：`https://github.com/AugustusHsu/agent-devflow/pull/37`（驗證專用 draft PR，空 commit `ff4428f`，驗完即關不合併）——orchestrator 以填好的模板檔 `gh pr create --draft --base main --body-file` 建立後**未經任何 edit** 即讀回：`baseRefName` = `main`，`secs` = `["基準","對應 AC","驗證","Write scope 自查"]`，`head_ok` = `true`，`lastEditedAt` = `null`（PR 關閉後讀回仍為 `null`；對照 PR #36 曾 edit，讀回 `2026-09-10T21:06:45Z`），body diff 僅第 25 行檔尾空行；比對用的 body-file 未保留在 repo，第三者重跑時自備填好的模板檔並保留至比對完成。取證用 PR 會永久留在 `gh pr list --state all`，不影響 `--state merged` 與 main；日後同類取證改在可丟棄 repo 進行） |
| 審查證據（`R3`／`R5`） | reviewer | `gh pr comment <N> --body "<審查結論；body 內寫明 head sha>"`；`gh pr review --approve\|--request-changes` 在單人 repo 不可用（本檔「已知限制」第一條） | ✅ 實測 2026-09-11（值欄依 `R7` 改寫：原列 `gh pr review <N> --approve\\|--request-changes --body` 與「review 原生綁 commit」，但同一帳號不能核可自己的 PR，單人 repo 走不到該指令；實際生效的機制是 `gh pr comment` 寫審查結論、body 寫明 head sha 以補回 review 原生的 commit 綁定（接 `R3`）。驗證方式（與「合併」列同口徑：讀回第三者現在仍可查的持久成品）：`gh pr view 34 --json comments,headRefOid --jq '{head: .headRefOid[0:7], last: (.comments \| map(select(.body \| test("^## 第.+輪審查："))) \| last \| {url, first: (.body \| split("\n")[0])})}'` 期望 `last.first` 含 `APPROVE` 或 `REQUEST_CHANGES` 且含 `head` 的 7 碼，實際輸出 `{"head":"51dce7d","last":{"url":"https://github.com/AugustusHsu/agent-devflow/pull/34#issuecomment-5624993819","first":"## 第五輪審查：`APPROVE` @ `51dce7d`"}}`。寫入動作本身：對任一有 write 權限的 PR 執行 `gh pr comment <PR-N> --body "<結論> @ <head sha>"`，記下 stdout 的 comment URL，以固定 ID 讀回 `gh api repos/<o>/<r>/issues/comments/<ID> --jq '{url: .html_url, first: (.body \| split("\n")[0])}'` 期望 `first` 含結論與 `gh pr view <PR-N> --json headRefOid` 前 7 碼。orchestrator 實跑於 PR #37：stdout `https://github.com/AugustusHsu/agent-devflow/pull/37#issuecomment-5625433360`，讀回 `first` = `REQUEST_CHANGES @ `ff4428f`（R8 通道驗證…）`，`headRefOid[0:7]` = `ff4428f`。受測環境：2026-09-11；gh 2.97.0；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入，讀回只需 pull、寫入需 write；目標 repo `AugustusHsu/agent-devflow`（公開、單人）；證據：寫入 `https://github.com/AugustusHsu/agent-devflow/pull/37#issuecomment-5625433360`（驗證用，PR #37 已關）；真實審查留言例 PR #34 `https://github.com/AugustusHsu/agent-devflow/pull/34#issuecomment-5624993819`） |
| 合併（`I3`） | approver／coordinator（`M2`） | `gh pr merge <PR-N> --merge --subject "<gitmoji> merge(#<issue-N>): …" --body "PR #<PR-N>; reviewer …; head <sha>"` | ✅ 實測 2026-09-11（依 `R9` 自原「合併（`I3`）」一格拆出：本格只宣稱合併指令與其三個選項的效果，原值欄的「repo 層：`allow_squash_merge=false`、`allow_rebase_merge=false`、`delete_branch_on_merge=true`」是本 repo 的設定，見本機節「repo 層合併設定」格。值欄依 `R7` 改寫一處：原寫死 `--subject "🔀 merge(#N): …"`，但讀回 PR #34 的 merge commit subject 實為 `📝 merge(#33): …`（`git log --oneline --merges -8 origin/main`：#2／#7／#14／#18／#28 用 🔀，#4／#30／#33 用該次變更本身的 gitmoji），實際生效的機制是 `--subject` 原樣接受任意字串、gitmoji 並非固定值，故改為 `<gitmoji>`。驗證方式兩項全驗（原第 (2)(3) 項；原第 (1) 項是 repo 設定讀回，隨該事實移到本機節）：(2) `--merge` 確實產生兩親 merge commit——`gh pr view 34 --json mergeCommit --jq .mergeCommit.oid` 得 `8029dfa12092701c3a6807405e2b047d036b1745`，`git cat-file -p 8029dfa12092701c3a6807405e2b047d036b1745 \| grep -n '^parent'` 期望兩行，實際 `2:parent 333d087c9f529cddcf04d00db587c7a52c50cea8` 與 `3:parent 51dce7d6ec33d7b3d38240925342dcddda9024d8`，第二個 parent 等於 `gh pr view 34 --json headRefOid --jq .headRefOid` 的 `51dce7d6ec33d7b3d38240925342dcddda9024d8`（逐行定位，不以計數為唯一依據）；(3) `--subject`／`--body` 生效——`git log -1 --format='%s%n%b' 8029dfa12092701c3a6807405e2b047d036b1745` 實際輸出 subject `📝 merge(#33): hermes.md 派工／worktree／中斷交接三格結帳，HITL 降 ⬜`、body 首行 `PR #34; reviewer codex(gpt-5.6-sol) ×5; head 51dce7d`，三段與值欄 body 的 `PR #N; reviewer …; head <sha>` 逐段對應；此處讀的是該兩個選項**產出的成品**，不是拿 commit 訊息的自述當證據。`gh pr merge` 無法對已合併 PR 重跑，故依 `R8` 採讀回成品的前瞻讀法——(2)(3) 對本 repo 任一已合併 PR 皆可照做，第三者現在可重現。受測環境：2026-09-11；gh 2.97.0；`grep` 為 GNU grep 3.11；git 2.43.0（orchestrator 於主 checkout 實測 `git version 2.43.0`；coder 白名單無 `git --version`）；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），(2)(3) 只需 pull ＋ 本機有該 repo 的 clone；目標 repo `AugustusHsu/agent-devflow`；證據：上述指令輸出 ＋ merge commit `8029dfa12092701c3a6807405e2b047d036b1745`） |
| 已合併訊號（`C1`） | coordinator | `gh pr view <N> --json state,mergedAt,mergeCommit` ＋ `git merge-base --is-ancestor` | ✅ 實測 2026-09-11（驗證方式兩項全驗：(1) forge 端——`gh pr view 34 --json state,mergedAt,mergeCommit --jq '{state, mergedAt, mergeCommit: .mergeCommit.oid}'` 期望 `state` 為 `MERGED`、`mergedAt` 非 null、`mergeCommit` 為 sha，實際輸出 `{"mergeCommit":"8029dfa12092701c3a6807405e2b047d036b1745","mergedAt":"2026-09-10T20:27:56Z","state":"MERGED"}`；(2) 本地 git 端獨立確認——`gh pr view 34 --json headRefOid --jq .headRefOid` 取 head `51dce7d6ec33d7b3d38240925342dcddda9024d8`，執行 `git merge-base --is-ancestor 51dce7d6ec33d7b3d38240925342dcddda9024d8 origin/main` 期望 exit 0，實際 exit 0；同一次以 `git rev-parse origin/main` 讀回 `8029dfa12092701c3a6807405e2b047d036b1745`，確認比對的是當下 main。兩項互補且缺一不可：(1) 只是 forge 自述，反例「PR 標 `MERGED` 但 head 不在 main 的歷史裡」要靠 (2) 擋下。第三者現在可對本 repo 任一已合併 PR 照做（本機需先有 `origin/main` 的 fetch）。受測環境：2026-09-11；gh 2.97.0；git 2.43.0（orchestrator 於主 checkout 實測 `git version 2.43.0`；coder 白名單無 `git --version`）；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），兩項實際只需 pull；目標 repo `AugustusHsu/agent-devflow`；證據：上述指令輸出） |
| 合併後刪分支（`C1`） | coordinator | repo 設定 `delete_branch_on_merge=true` 開啟時，合併由 GitHub 自動刪遠端分支；`git ls-remote --heads origin <branch>` 驗 | ✅ 實測 2026-09-11（依 `R9` 自原「合併後刪分支（`C1`）」一格拆出：本格只宣稱 GitHub 的自動刪分支機制與讀回方式，「本 repo 的 `delete_branch_on_merge` 為 `true`」是本 repo 的設定，其讀回（原第 (1) 項）併入本機節「repo 層合併設定」格。值欄依 `R7` 改寫：原列 `gh pr merge --delete-branch` 或 `git push origin --delete <branch>` 兩法皆未實測，本 repo 今日的合併都沒有帶 `--delete-branch`，實際生效的是 repo 設定 `delete_branch_on_merge=true`，故改寫後只留該機制、不沿用原宣稱。驗證方式：(2) 機制生效——`gh pr view 34 --json headRefName --jq .headRefName` 取已合併 PR 的分支名 `33-hermes-cells`，`git ls-remote --heads origin 33-hermes-cells` 期望無輸出，實際無輸出；為排除「遠端連不上時同樣是空輸出」這個反例，同一次另跑 `git ls-remote --heads origin`，實際只輸出兩行——`b791d4d408c8ae7a3dd1e8ba4b896a62b0d8d94b` → `refs/heads/4-lookup-table-settlement`、`8029dfa12092701c3a6807405e2b047d036b1745` → `refs/heads/main`，證明遠端可連通且 `33-hermes-cells` 確實不在其中。第三者現在可對本 repo 任一已合併 PR 照做；換 repo 時以該 repo 的已合併 PR 為參數，且該 repo 的 `delete_branch_on_merge` 須先為 `true`（本 repo 的讀值見本機節「repo 層合併設定」格）。受測環境：2026-09-11；gh 2.97.0；git 2.43.0（orchestrator 於主 checkout 實測 `git version 2.43.0`；coder 白名單無 `git --version`）；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），(2) 只需 pull；目標 repo `AugustusHsu/agent-devflow`；證據：上述指令輸出） |
| CI 位置 | — | `.github/workflows/` | ✅ 實測 2026-09-11（驗證方式兩項全驗：(1) 檔案位置——在該 repo 的工作目錄執行 `ls .github/workflows/` 期望列出 workflow 檔，實際輸出 `devflow-checks.yml`；(2) 該位置確實被 GitHub 讀取並執行——`gh run list --workflow=devflow-checks.yml --limit 1` 期望至少一筆 run，實際輸出一列（tab 分隔）`completed`、`success`、`📝 process(對照表): hermes.md 派工／worktree／HITL／中斷交接四格結帳`、`devflow checks`、`33-hermes-cells`、`pull_request`、`34526392736`、`11s`、`2026-09-10T20:26:31Z`。(1) 單獨只證「檔案在那個路徑」，反例「檔案放對但 GitHub 沒在跑」要靠 (2) 擋下。第三者現在可照做。受測環境：2026-09-11；gh 2.97.0；`ls` 為 GNU coreutils 9.4；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），(1) 需本機有該 repo 的 checkout、(2) 只需 pull；目標 repo `AugustusHsu/agent-devflow`；證據：上述兩條指令輸出 ＋ run `34526392736`） |
| 人類站（`D3`） | — | GitHub Pages：`gh-pages` 分支或 Actions 部署 | ⬜ 未實測 |
| 故障退路（`F3`） | coordinator | `F3`（forge 回錯或逾時）在 GitHub 的讀回程序：尚未推導 | ⬜ 未實測（值欄依 `R9` 自「—」改寫，只補上宣稱對象，未推導內容） |

## 本機

| 面向 | 職位 | 值 | 狀態 |
|---|---|---|---|
| CLI（本機 instance） | — | `gh` 本機 2.97.0 | ✅ 實測 2026-09-11（依 `R9` 自原「CLI」一格拆出：本格宣稱的是本機安裝的 `gh` 這個 instance；「GitHub 的 CLI 是 `gh`」見通用節「CLI」格。驗證方式兩項全驗：(1) `gh --version` 期望首行 `gh version 2.97.0 (2026-07-31)`，實際輸出 `gh version 2.97.0 (2026-07-31)` 與次行 `https://github.com/cli/cli/releases/tag/v2.97.0`，與值欄「本機 2.97.0」相符；(2) `gh auth status` 期望顯示已登入，實際輸出 `✓ Logged in to github.com account AugustusHsu (keyring)`、`Active account: true`、`Git operations protocol: ssh`、`Token scopes: 'gist', 'project', 'read:org', 'repo'`。第三者現在可照做。受測對象是**本機 CLI 環境**，與 repo 無關——換機器或 `gh` 升版即須依 `R10` 重驗。受測環境：2026-09-11；gh 2.97.0；執行身分為本機安裝 `gh` 並完成 `gh auth login` 的 OS 使用者（`gh --version` 不需任何 repo 權限，`gh auth status` 只讀本機 keyring）；目標 repo：不適用；證據：上述兩條指令輸出） |
| 標準 label | — | 本 repo（`AugustusHsu/agent-devflow`）已建的 label：`spec`、`process`、`bypass`、`blocked` | 📝 已宣稱（驗證未達 `✅`：`R10` 未附齊）。來源具名：AugustusHsu，2026-09-09——issue #1 本體 AC-2 宣稱 Phase 1 已以 `gh label list` 驗過，寫入 commit `6eb62d4`。原記載保留：實測 2026-09-09（`gh label list` 列出 spec／process／bypass／blocked）。該驗證方式第三者現在可執行，但狀態欄缺 `R10` 的所用工具與版本、執行身分或權限、目標 repo 記載（值欄補的 repo 名不替代狀態欄記載），不滿足 `✅` 成立條件；本單（issue #66）禁止重測，故依 `R9` 降級而非補跑。升回 `✅` 須重跑 `gh label list` 並附齊 `R10` 各項 |
| 分支保護 | — | 本 repo（`AugustusHsu/agent-devflow`）的 main：必經 PR、禁 force push、dismiss stale approvals、required approvals 0 | ✅ 實測 2026-09-11（原五項中的「只允許 merge commit」讀的是 repo 層 `allow_merge_commit`／`allow_squash_merge`／`allow_rebase_merge`，不是 branch protection，與下一格「repo 層合併設定」是同一事實，依 `I5` 連同其 repo API 驗證項移至該格；本格只留 branch-protection 端點的四項，一條指令一次讀回，第三者現在照跑即可重現。受測環境：2026-09-11，gh 2.97.0，執行身分為對 `AugustusHsu/agent-devflow` 具 admin 權限的 `gh` 登入——protection 端點對非 admin 回 404；目標 repo `AugustusHsu/agent-devflow`。驗證方式：`gh api repos/AugustusHsu/agent-devflow/branches/main/protection --jq '{pr: (.required_pull_request_reviews != null), force: .allow_force_pushes.enabled, dismiss: .required_pull_request_reviews.dismiss_stale_reviews, approvals: .required_pull_request_reviews.required_approving_review_count}'` 期望輸出 `{"approvals":0,"dismiss":true,"force":false,"pr":true}`，四鍵依序對應 required approvals 0／dismiss stale approvals／禁 force push／必經 PR） |
| repo 層合併設定 | — | 本 repo（`AugustusHsu/agent-devflow`）的 repo 層設定：`allow_merge_commit=true`、`allow_squash_merge=false`、`allow_rebase_merge=false`、`delete_branch_on_merge=true` | ✅ 實測 2026-09-11（依 `R9` 自原「合併（`I3`）」一格拆出（原值欄「repo 層：…」三項）；原「合併後刪分支（`C1`）」一格的第 (1) 項讀的是同一事實中的 `delete_branch_on_merge`，依 `I5` 併入本格，不在兩處各留一份；原「分支保護」一格的「只允許 merge commit」及其 repo API 驗證項（G 版該格第 2 項）讀的也是同一事實，同依 `I5` 移入本格，值欄因此明列 `allow_merge_commit=true`。驗證方式三項全驗：(1) repo 四設定——`gh api repos/AugustusHsu/agent-devflow --jq '{allow_merge_commit,allow_squash_merge,allow_rebase_merge,delete_branch_on_merge}'` 期望 `allow_merge_commit` 為 `true`、`allow_squash_merge`／`allow_rebase_merge` 為 `false`、`delete_branch_on_merge` 為 `true`，實際輸出 `{"allow_merge_commit":true,"allow_rebase_merge":false,"allow_squash_merge":false,"delete_branch_on_merge":true}`（期望值新增 `allow_merge_commit` 一項以對齊值欄，實際輸出未變、本就含該鍵）；(1) 與 PR 無關。(2) 機制存在（原「合併後刪分支」第 (1) 項）——`gh api repos/AugustusHsu/agent-devflow --jq .delete_branch_on_merge` 期望 `true`，實際 `true`。(3) 只允許 merge commit（自「分支保護」移入；該項受測環境與本格相同：2026-09-11，gh 2.97.0，對 `AugustusHsu/agent-devflow` 具 admin 權限的 `gh` 登入，目標 repo `AugustusHsu/agent-devflow`）——`gh api repos/AugustusHsu/agent-devflow --jq '{merge: .allow_merge_commit, squash: .allow_squash_merge, rebase: .allow_rebase_merge}'` 期望輸出 `{"merge":true,"rebase":false,"squash":false}`＝只允許 merge commit。第三者現在可對本 repo 照做。受測環境：2026-09-11；gh 2.97.0；執行身分為對 `AugustusHsu/agent-devflow` 具 admin 的 `gh` 登入（`gh api repos/AugustusHsu/agent-devflow --jq .permissions` 輸出 `{"admin":true,"maintain":true,"pull":true,"push":true,"triage":true}`），讀 repo 合併設定需 admin；目標 repo `AugustusHsu/agent-devflow`；證據：上述指令輸出） |

## 已知限制（文件推導，待實測）

- 同一帳號不能核可自己的 PR；單人 repo 的 required approvals 必須是 0，審查證據靠 review 內容與 head sha，不靠 approval 計數。
- REST `/issues` 會混入 PR，列 issue 時要過濾。
- 公開 repo 預設任何人可送 review；必要時開 code review limits。
