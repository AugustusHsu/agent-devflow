# coder: codex

衍生值對照表，**只有通用節**（值欄宣稱可重用的工具能力或程序，repo 與環境只是可替換的參數）。本機節——具名的環境 instance——住 `devflow.local/<同路徑>`，不隨 kit 安裝。分節與狀態欄依 `R9`。

## 通用

| 面向 | 職位 | 值 | 狀態 |
|---|---|---|---|
| headless 執行（`L2`） | implementer | `codex exec -C <worktree> -m <model> -c model_reasoning_effort=<level> --sandbox workspace-write --json -o <last.md> - < <prompt.txt>`；`--json` 取事件流、`-o <file>` 取最後訊息；末尾的 `-` 表 prompt 由 **stdin** 餵入；不需 PTY | ✅ 可用（實測 2026-09-21，升自 `📝`；既有 `📝` 記載原文保留於後段。`✅` 成立條件：驗證方式已在**第二個 repo** 由**不同任務**以值欄指令**逐字**（無任何額外旗標）重跑，三點全部再現，三點的**原文**（事件流首行 JSON、`-o` 檔全文、stdin／無 PTY 說明）與完整啟動指令記在 forge 留言，受測環境（`R10`）附齊。實跑：消費者 repo `AugustusHsu/devflow-e2e` issue #5，指令 `codex exec -C <worktree> -m gpt-5.6-sol -c model_reasoning_effort=medium --sandbox workspace-write --json -o /tmp/e2e-coder5-last.md - < /tmp/e2e-coder5.txt`：(1) 首行 `{"type":"thread.started","thread_id":"01a0c1da-a22c-7de0-a701-89c481684d65"}`；(2) `-o` 檔全文 `DONE / commit: e86a486… / rows: 5 ✅ / 0 📝`；(3) prompt 由 `- < file` 重導入、程序在 `systemd-run --scope` 下無控制終端；exit 0。產物 commit `e86a486` → PR #6 合併 `6a3da96`。附帶：(a) `-C` 指向 `git worktree`（`.git` 為檔案）時 Codex 0.149.1 **不需** `--skip-git-repo-check`，本次即無該旗標；(b) `-c model_reasoning_effort=medium` 不加引號被接受；(c) 舊 `📝` 記的「`~/.codex/AGENTS.md` commit 前徵同意會蓋過派工 prompt」在 prompt 明寫「orchestrator 已預先同意 commit」時未再現，與該段建議一致。受測環境：2026-09-21，`codex-cli 0.149.1`、模型 `gpt-5.6-sol`／reasoning medium；執行身分為本機使用者以 Codex 訂閱登入；目標 repo 私有 `AugustusHsu/devflow-e2e`，worktree `devflow-e2e.worktrees/5`。證據：https://github.com/AugustusHsu/devflow-e2e/issues/5#issuecomment-5754746924（三點原文）、https://github.com/AugustusHsu/devflow-e2e/pull/6。—— 以下為原 `📝` 記載：驗證方式：以值欄的指令對一個既有 worktree 派任一任務，prompt 寫在 `<prompt.txt>` 由 stdin 餵入；看三點——(1) `--json` 的事件流第一行為 `thread.started` 且帶 `thread_id`；(2) `-o` 指定的檔案在結束後含該次的最後訊息；(3) 全程無 PTY（stdin 是重導入的檔案）。實跑：orchestrator 於 #110 以 `codex exec -C <worktree> -m gpt-5.6-sol -c model_reasoning_effort=high --sandbox workspace-write --json -o <last.md> - < prompt.txt` 派 Codex 首次擔任 implementer，thread `01a0bdd3-bbf9-7010-9bcb-08c8d898e83b`，18 個指令、5 則訊息，exit 0，產物即 PR #111。**`~/.codex/AGENTS.md` 的「commit 前徵同意」會蓋過派工 prompt 的 commit 指示，coder 停在 commit 前**；需 `codex exec resume <id>` 授權，或在 prompt 裡明寫「本次 orchestrator 已預先同意 commit」。這是 headless 用法的實際約束，與 `L3` 停無關（見本表 HITL 格）。受測環境：2026-09-20，`codex-cli 0.149.1`、模型 `gpt-5.6-sol`／reasoning high；執行身分為本機使用者以 Codex 訂閱登入；目標 repo `AugustusHsu/agent-devflow`，worktree `agent-devflow.worktrees/110`。**為何不是 `✅`**：驗證由 orchestrator 執行並回報，尚無第三者獨立重跑。證據：https://github.com/AugustusHsu/agent-devflow/issues/110#issuecomment-5748564171） |
| 入口檔（`D2`） | — | `AGENTS.md` | ⬜ 未測（2026-09-20 嘗試驗證（#106 AC-3）：審查 prompt 刻意不提 `D4`、審查對象含 162 KB 的 `scripts/devflow_checks.py`，事後查事件流看審查者首次接觸該檔的方式——事件流裡它碰該檔只有兩次、都是執行（`python3 scripts/devflow_checks.py`、`python3 tests/smoke_devflow_checks.py`），從沒讀它，無「先 `grep -n` 還是直接讀」的信號，分不出遵從來源。要驗這格得設計一個審查對象本身就是大檔的任務，見 https://github.com/AugustusHsu/agent-devflow/issues/106#issuecomment-5748457773） |
| 權限 | — | `--sandbox read-only\|workspace-write` 的隔離**依賴 host 能建 user namespace**（Codex 的 Linux 沙箱用 bubblewrap 實作）；建不起來時 Codex 不拒絕執行、也不停下診斷，而是印 `warning: Codex's Linux sandbox uses bubblewrap and needs access to create user namespaces.` 後**照跑，等同 `danger-full-access`**；`workspace-write` 模式連這行 warning 都不印，靜默無隔離。host 能建 user namespace 時隔離成立：`read-only` 下 workdir 內外的寫入皆被 `Read-only file system` 拒；`workspace-write` 的可寫根**預設是 workdir ＋ `/tmp` ＋ `$TMPDIR`**，不是只有 workdir（`codex exec` 開頭的 header 會印 `sandbox: workspace-write [workdir, /tmp, $TMPDIR]`），可寫根**外**的寫入被拒 | ✅ 可用（驗證方式：**先在 host 上跑 `bwrap --unshare-user --bind / / true` 確認 host 條件**，失敗則本格前提不成立、以下結果不會重現。再對乾淨 `/tmp` clone 執行 `codex exec -C <clone> --sandbox read-only -o <out> - < <probe.txt>`，probe prompt 依序要求 (1) 讀一個檔 (2) 跑 `git log` (3) 寫 clone **外**的 `/tmp/ro-probe-out.txt`（**此路徑須與 `<probe.txt>` 不同**——同路徑會自撞，檔案必然存在，判不出有沒有落地） (4) 寫 clone **內**一檔，並對 (3)(4) 各跑一次 `ls -l` 回報；隔離成立則 (3)(4) 皆被拒、兩檔皆不存在。`workspace-write` 再跑一次：因 `/tmp` 在該模式下本來就可寫，(3) 要改成寫**可寫根外**的路徑（例如 `$HOME` 下的一個檔）才測得到邊界，預期 (3) 拒、(4) 成功。判落地一律由派工者自己 `ls`／讀檔確認，不採信模型回報。實跑：orchestrator 於 `332ab9d` 首次跑，四步全 `ok`，`/tmp/ro106-probe.txt` 與 `/tmp/ro106/PROBE.md` 皆真的落地——**隔離不成立**；同一 host 直接跑 `bwrap` 即失敗：`setting up uid map: Permission denied`／`loopback: Failed RTM_NEWADDR: Operation not permitted`，故該 host 上兩模式皆無隔離。**根因與修法**：Ubuntu 24.04 的 `kernel.apparmor_restrict_unprivileged_userns=1` 擋掉沒有 AppArmor profile 的 `bwrap`；裝 `/etc/apparmor.d/bwrap`（`flags=(unconfined)` ＋ `userns` 授權，只給 bwrap 一個 profile 名字，沿 Ubuntu `lxc-usernsexec` 的模式）後 `bwrap --unshare-user --bind / / true` 成功。修復後於 `b9f985a` 的 `/tmp/ro127` clone 重跑：`read-only` 的 (1)(2) `ok`、(3)(4) 皆 `/bin/bash: …: Read-only file system`，派工者自查 `<probe.txt>` 內容原封未被覆寫、clone 內 `PROBE.md` 不存在；`workspace-write` 的 clone 內 `PROBE.md` 與 `/tmp` 下的檔皆寫成（兩者都在可寫根內），改寫 `$HOME` 下的路徑則 `Read-only file system` 且檔案不存在——兩模式邊界皆與值欄相符。受測環境（`R10`）——**首次驗證**：2026-09-20，`codex-cli 0.149.1`、`bwrap 0.9.0`（該次為負結果）；**最近確認**：2026-09-21，`codex-cli 0.149.1`、`bwrap 0.9.0`，coder session `2a538534`。兩次共同條件：模型 `gpt-5.6-sol`／reasoning low；執行身分為本機使用者以 Codex 訂閱登入；目標 repo `AugustusHsu/agent-devflow` 的 `/tmp` clone。**host 條件：`bwrap --unshare-user --bind / / true` 須成功**——這是本格成立的前提，host 的 AppArmor／`kernel.apparmor_restrict_unprivileged_userns`／userns 相關設定變更使該條件不再成立時，本格依 `R10` 降為 `📝`。證據：https://github.com/AugustusHsu/agent-devflow/issues/127#issuecomment-5751267413 ；負結果原始紀錄 https://github.com/AugustusHsu/agent-devflow/issues/106#issuecomment-5748372216） |
| worktree（`I2`） | coordinator（建）／implementer（不自建） | 由 orchestrator 建 | 📝 已宣稱（`R9` 子類「驗證未達 `✅`」：本格附有第三者現在能執行的驗證方式，實跑結果與值欄宣稱相符，但不滿足 `✅` 的成立條件，理由見末段。驗證方式：orchestrator 先以 `git worktree add` 建好 worktree，coder 以 `codex exec -C <該路徑>` 進入；事後 grep 該次 `--json` 事件流裡的全部指令，`git worktree`、`git checkout -b`、`git switch` 三者皆無命中，即 coder 未自建 worktree 或分支。實跑：#110 派工的 18 個指令中三者皆 0 命中，Codex 全程在 orchestrator 預建的 worktree 內工作。受測環境：2026-09-20，`codex-cli 0.149.1`、模型 `gpt-5.6-sol`／reasoning high；執行身分為本機使用者以 Codex 訂閱登入；目標 repo `AugustusHsu/agent-devflow`，worktree `agent-devflow.worktrees/110`。**為何不是 `✅`**：驗證由 orchestrator 執行並回報，尚無第三者獨立重跑。證據：https://github.com/AugustusHsu/agent-devflow/issues/110#issuecomment-5748564171） |
| HITL（`L3`） | implementer | headless 無互動 → issue 留言後停 | ⬜ 未測（2026-09-20 嘗試驗證（#110）：派工 prompt 未明令「留言後停」，只指向 `L3` 原文；coder 讀了 `L3`（事件流引用 23 次）、讀了三張 PR 的紀錄，自判「比對結果已足以收斂」後直接改，未留言。**本任務的未決事項強度不足以區分「判斷不需停」與「不會停」**——它的判斷可能是對的。需未決事項更明確（處置明顯落在 write scope 外、或 G／T 明顯衝突）的載體才能驗，見 https://github.com/AugustusHsu/agent-devflow/issues/110#issuecomment-5748564171） |
| 審查用法（`R1`） | reviewer | 於乾淨 checkout 執行 `codex exec -m <model> -c model_reasoning_effort=<level> --sandbox workspace-write -o <verdict.md> - < <prompt.txt>`；末尾的 `-` 表 prompt 由 **stdin** 餵入。checkout 以 `git clone --shared` 到 `/tmp` 後 `git checkout <head sha>`。`--shared` clone 的 origin 是本機路徑，`gh` 無法自動推導 repo（錯誤：`none of the git remotes … point to a known GitHub host`）——須 `git remote set-url origin` 成 GitHub remote（HTTPS 或 SSH），或以 `gh --repo <owner/repo>`／`GH_REPO` 明示。`--skip-git-repo-check` 在此**非必要**：它只放行「非 git 目錄」（在非 git 目錄不加它會擋：`Not inside a trusted directory and --skip-git-repo-check was not specified`），而 `/tmp` checkout 本身是 git repo | 📝 已宣稱（有可執行的驗證方式：本 repo #80→#105 的 Codex 審查全以此形式實跑，verdict 貼於各 PR；來源 orchestrator（Hermes）。`R9` 子類「驗證未達 `✅`」——受測環境記載與第三者重跑證據待 #106 第二輪補齊後再判能否升 `✅`。原值欄並列的 `codex exec review` 依 `R7` 拆成下一列） |
| 審查用法：`review` 子命令（`R1`） | reviewer | `codex exec review`——Codex 內建的 code review 子命令 | ⬜ 未測（子命令存在已驗：`codex exec --help` 於 `codex-cli 0.149.1` 列出 `review  Run a code review against the current repository`。但**未驗證**它能否讀自訂 review prompt、能否輸出到指定檔；本 repo 至 `332ab9d` 止從未用過。依 `R7` 與上一列拆開各自標狀態，不共格，見 #106 AC-1） |
| 內容過濾 | reviewer／implementer | OpenAI 端的內容安全過濾會中止 `codex exec`：訊息原文 `ERROR: This content was flagged for possible cybersecurity risk`，exit 1、`-o` 指定的檔案無輸出（拿不到 verdict）。已知觸發主題：**重跑「symlink 越界刪除」的注入腳本**——`#137` review-138b 連續兩次命中（65k tokens 後中止）。攔的是該主題本身，不是 Codex 或 sandbox 的限制；同主題的第一輪未被攔，差別在當時腳本是審查者**自己逐步寫成**，而非把寫好的腳本整份餵進去。處置：改由 orchestrator 代跑該腳本、把執行結果當輸入交給審查者，審查者只做靜態閱讀＋跑既有 harness（review-138c 即以此完成） | 📝 已宣稱（`R9` 子類「無可執行的驗證方式」：過濾是 OpenAI 端的非確定性判定，同一份內容不保證再現、也無從由外部觸發或關閉，第三者沒有能執行的驗證方式——不論先前觸發過幾次，依 `R9` 不得以回報次數升 `✅`，也不得據本格宣告「某類審查會被擋」而略過該審查。來源具名：orchestrator（Hermes）於 #137 的紀錄 https://github.com/AugustusHsu/agent-devflow/issues/137#issuecomment-5752718058 ，內含兩次中止的說明、代跑腳本 `/tmp/vd138_rmdir_race.py` 於 `62e77be` 的輸出原文（`exit=3`、`outside_deep_lexists=True`、stderr `devflow/zz/deep: symlink appeared under devflow/ during the write phase`）與 review-138c 的重派指令全文） |
| 交接 | implementer（context 延續）／coordinator（重派） | `codex exec resume <id>` | 📝 已宣稱（`R9` 子類「驗證未達 `✅`」：本格附有第三者現在能執行的驗證方式，重跑結果與值欄宣稱相符，但不滿足 `✅` 的成立條件，理由見末段。驗證方式：任一 `codex exec --json` 取事件流第一行 `thread.started` 的 `thread_id`；以 `codex exec resume <該 id> --json` 接回，看第二次事件流第一行的 `thread_id` 是否與第一次相同；第二段 prompt 只引用第一段的內容而不重給，看回覆是否逐項對應。實跑：orchestrator 於 PR #107 第一輪審查刻意分兩段，三個獨立來源——第一段事件流第一行、審查者自報的 `SESSION_ID:`、第二段 `resume` 的事件流第一行——同一 id `01a0bdb9-1001-7113-b8d2-348a30e16703`；第二段 prompt 未重給 diff、未重列疑慮內容，產出的 verdict 逐項對應第一段的五項疑慮並各判阻擋／非阻擋，context 確實延續。受測環境：2026-09-20，`codex-cli 0.149.1`、模型 `gpt-5.6-sol`／reasoning high；執行身分為本機使用者以 Codex 訂閱登入；目標 repo `AugustusHsu/agent-devflow`，checkout `/tmp/rv107` 於 `f73b935`。**為何不是 `✅`**：驗證由 orchestrator 執行並回報，尚無第三者獨立重跑。證據：https://github.com/AugustusHsu/agent-devflow/issues/106#issuecomment-5748457773 ；PR #107 第一輪 verdict https://github.com/AugustusHsu/agent-devflow/pull/107#pullrequestreview-5259971160 ，其內文自載 `SESSION:` 行，第三者可與該留言比對） |
