# coder: claude-code

衍生值對照表。分節與狀態欄依 `R9`：**通用**節的值欄宣稱可重用的工具能力或程序（repo 與環境只是可替換的參數），**本機**節的值欄宣稱具名的環境 instance（本機安裝的工具、帳號或組織、本 repo 的設定與資源）；狀態取 `✅`／`📝`／`⬜` 三值之一，兩節的 `✅` 判準各依 `R9` 定義，非 `✅` 不得在流程中當作可用。「職位」欄記該格是哪個職位的用法（詞彙見 `seats/`），`—` 表示是工具或資源本身的性質，不專屬任一職位。

## 通用

| 面向 | 職位 | 值 | 狀態 |
|---|---|---|---|
| headless 執行（`L2`） | implementer | 在 worktree 內：`claude -p "<prompt>" --output-format json --max-turns <N>`；stdout 為單一 JSON | ✅ 可用（實測 2026-09-11，驗證方式：在既有 worktree 內執行 `claude -p "<任意 prompt>" --output-format json --max-turns <N>`，stdout 為單一 JSON 物件，含 `session_id`（字串）、`num_turns`（數字）、`result`（字串，最終回覆全文）三個鍵；此程序不依賴任何特定 prompt 或 repo，第三者現在可獨立重跑。受測環境：2026-09-11，Claude Code 2.1.267 headless，權限為 `--allowedTools` 白名單、未用 `--dangerously-skip-permissions`，目標 repo `AugustusHsu/agent-devflow`；證據：issue #4 留言 https://github.com/AugustusHsu/agent-devflow/issues/4#issuecomment-5622874599） |
| 入口檔（`D2`） | — | `CLAUDE.md` | ⬜ 未測 |
| 權限：`--allowedTools` | — | `--allowedTools '<pattern list>'` 白名單；不在名單的呼叫被拒，該次呼叫記入 stdout JSON 頂層的 `permission_denials`（每筆含 `tool_name`、`tool_input`） | 📝 已宣稱（`R9` 子類「驗證未達 `✅`」：本格附有第三者現在能執行的驗證方式，實跑結果與值欄宣稱相符，但不滿足 `✅` 的成立條件，理由見末段。驗證方式：派任何 `claude -p "<prompt>" --output-format json --allowedTools '<窄名單>'`，prompt 要求的動作需用到名單外的工具形狀；讀 stdout JSON 頂層的 `permission_denials` 陣列，看兩點——(1) 該陣列非空，每筆含 `tool_name` 與 `tool_input`；(2) 被拒的呼叫形狀確實不在 `--allowedTools` 名單內。實跑：#114 第一段派工的 coder session，名單外的 `Bash(gh issue comment *)` 呼叫被拒，coder 端表現為該指令未執行、回 `This command requires approval`。受測環境：2026-09-20，Claude Code `2.1.277` headless，執行身分為本機使用者以 Anthropic 訂閱登入，目標 repo `AugustusHsu/agent-devflow`。**為何不是 `✅`**：(a) 證據為 orchestrator 自派工輸出摘錄，尚無第三者獨立重跑；(b) 白名單不是唯一的放行判準，邊界未定——同一 session 內名單外的 `ls`、`echo` 單獨呼叫時未被拒而正常執行，同一個 `echo` 併入複合指令後卻被拒，名單外的 `ls` 指向工作目錄外時亦被拒；coder 端分不出被拒與否取決於名單本身、或另有唯讀與工作目錄範圍的自動放行途徑疊在名單之上，故值欄「不在名單的呼叫被拒」尚未涵蓋所有呼叫形狀。證據：`<#114 留言 URL，orchestrator 送審前補>`。原值欄並列的 `--permission-mode` 與 `--dangerously-skip-permissions` 依 `R7` 拆成下兩列） |
| 權限：`--permission-mode` | — | `--permission-mode <mode>` 指定該次執行的權限模式 | ⬜ 未測（本 repo 至 `544847e` 止每次派工都只給 `--allowedTools`，從未給過此選項，無任何具名來源回報做過。依 `R7` 與上一列拆開各自標狀態，不共格） |
| 權限：`--dangerously-skip-permissions` | — | 持憑證的環境不用 `--dangerously-skip-permissions` | ⬜ 未測（本 repo 至 `544847e` 止從未給過此選項——「不用」是本 repo 既定做法的宣稱，非實測結果：未驗證此選項的實際行為，也未驗證持憑證環境下省略它的後果。依 `R7` 與上兩列拆開各自標狀態，不共格） |
| worktree（`I2`） | coordinator（建）／implementer（不自建） | 由 orchestrator 以 `git worktree add` 建；不用 `claude -w` | ✅ 可用（實測 2026-09-11，驗證方式：orchestrator 在主 checkout 執行 `git branch <N>-<slug> origin/main` 與 `git worktree add ../<repo>.worktrees/<N> <N>-<slug>`；啟動 coder 前以 `git worktree list` 確認輸出含該路徑與分支名，並逐字核對啟動指令不含 `-w`／`--worktree`。判定點：worktree 路徑在 repo 目錄之外，且 repo 內不存在 `.claude/worktrees/`。第三者現在可對任一 repo 重跑此程序。受測環境：2026-09-11，工具為本機 `git version 2.43.0` 的 `worktree` 子命令（orchestrator 於主 checkout 執行 `git --version`），orchestrator 具該 repo 的本機寫入權限，目標 repo `AugustusHsu/agent-devflow`；證據：issue #4 留言 https://github.com/AugustusHsu/agent-devflow/issues/4#issuecomment-5622810729） |
| HITL 通道 | implementer | headless 可用 `gh issue comment` 留持久紀錄，且 stdout `result` 可回 `BLOCKED:` 後正常結束 | ✅ 可用（實測 2026-09-11，本格只宣稱通道存在，不宣稱 coder 會自發遵守 `L3`——後者見下一列。驗證方式：以 headless `claude -p … --output-format json` 派一個含未決事項的任務，其 `--allowedTools` 含 `Bash(gh issue comment *)` 而不含任何 push／PR 工具；檢查三點皆成立——(1) 該 session 的 `gh issue comment` 成功，留言出現在對應 issue 上、第三者可獨立查得；(2) stdout JSON 的 `result` 以 `BLOCKED:` 開頭；(3) 該 session 結束時分支上沒有與該未決事項相關的 commit。第三者現在可用任一 issue 重跑此程序。受測環境：2026-09-11，Claude Code 2.1.267 headless，gh 2.97.0，git 2.43.0；執行身分為對 `AugustusHsu/agent-devflow` 具 write 權限的 `gh` 登入（留言需要），`--allowedTools` 白名單不含 push／PR 工具；目標 repo `AugustusHsu/agent-devflow`。證據：issue #4 留言 https://github.com/AugustusHsu/agent-devflow/issues/4#issuecomment-5622863707 ＋ https://github.com/AugustusHsu/agent-devflow/issues/4#issuecomment-5622874599（coder 第一段）；審查者獨立重跑：https://github.com/AugustusHsu/agent-devflow/issues/4#issuecomment-5623667680） |
| HITL（`L3` 遵從） | implementer | 遇未決事項會自發 issue 留言後停（非 prompt 明令） | ⬜ 未測（派工 prompt 若明令「留言後停」，依 `R8` 只證服從指令、不證自發遵從；需設計不含該指令的測試） |
| 審查用法（`R1`） | reviewer | 於乾淨的 `/tmp` checkout（切到待審 HEAD sha）執行 `claude -p "$(cat <prompt.txt>)" --output-format json --max-turns <N> --allowedTools '<list>'`；stdout 為單一 JSON，其 `result` 即 verdict 全文，由 orchestrator 貼到 PR | 📝 已宣稱（`R9` 子類「驗證未達 `✅`」：本格附有第三者現在能執行的驗證方式，實跑結果與值欄宣稱相符，但不滿足 `✅` 的成立條件，理由見末段。驗證方式：把待審 PR 的 HEAD sha checkout 到一個 `/tmp` 目錄，審查 prompt 寫在 `<prompt.txt>`，以值欄的指令啟動；看三點——(1) stdout 為單一 JSON 物件；(2) 其 `result` 為 verdict 全文（`VERDICT:` 開頭、含阻擋項與非阻擋觀察）；(3) 該 `result` 與 orchestrator 貼到 PR 上的 verdict 逐項一致。實跑：Claude 於 PR #111 首次擔任 reviewer，一輪 APPROVE，verdict 內文自載 `HEAD: b1bc0eb`，含逐句實測四句與六項非阻擋觀察，第三者可與該留言比對。受測環境：2026-09-20，Claude Code `2.1.277`（實作位當日在同一台主機執行 `claude --version` 所得，stdout 為 `2.1.277 (Claude Code)`；本表本機節「版本」格記的 `2.1.267` 係 2026-09-11 的記載，依 `R10` 兩者不同，引用前須重比對）；執行身分為本機使用者以 Anthropic 訂閱登入；目標 repo `AugustusHsu/agent-devflow`。**為何不是 `✅`**：驗證由 orchestrator 執行並回報，尚無第三者獨立重跑；且該次的 `--max-turns`／`--allowedTools` 具體值未隨 verdict 留存，第三者重跑須自行給定。證據：PR #111 verdict https://github.com/AugustusHsu/agent-devflow/pull/111#pullrequestreview-5260050219 。原值欄並列的 `gh pr diff \| claude -p` 依 `R7` 拆成下一列） |
| 審查用法：pipe diff（`R1`） | reviewer | `gh pr diff <N> \| claude -p "<review-prompt>"`——以 stdin 餵 diff、不開 checkout | ⬜ 未測（本 repo 至 `6f3656d` 止從未以此形式派過審查，無任何具名來源回報做過。依 `R7` 與上一列拆開各自標狀態，不共格，見 #112 AC-4） |
| 交接（`--resume`） | implementer（context 延續）／coordinator（重派） | `--output-format json` 回 `session_id`，可 `--resume <session_id>` 續同一 session | ✅ 可用（實測 2026-09-11，驗證方式：(1) 執行 `claude -p "<任意 prompt>" --output-format json`，記下 stdout JSON 的 `session_id`；(2) 在同一 worktree 以 `claude -p "<後續 prompt>" --resume <該 session_id> --output-format json` 再啟動一次；(3) 比對第二次 stdout 的 `session_id` 與第一次相同，且第二次的回應以第一次的 context 為前提（不需重述背景即可續做）。此程序不依賴特定 prompt，第三者現在可獨立重跑。受測環境：2026-09-11，Claude Code 2.1.267 headless，同一 worktree 路徑；第二次須由**同一 OS 使用者**執行（`--resume` 讀取該使用者 `~/.claude/` 下的 session 紀錄，換身分或清除該目錄即無法續接）；目標 repo `AugustusHsu/agent-devflow`；證據：issue #4 留言 https://github.com/AugustusHsu/agent-devflow/issues/4#issuecomment-5622874599 與 https://github.com/AugustusHsu/agent-devflow/issues/4#issuecomment-5623153788，兩者 `session_id` 相同） |
| 交接（`--continue`） | implementer（context 延續）／coordinator（重派） | 同目錄 `--continue` | ⬜ 未測 |

## 本機

| 面向 | 職位 | 值 | 狀態 |
|---|---|---|---|
| 版本 | — | 本機 2.1.277 | ✅ 可用（實測 2026-09-20，驗證方式：`claude --version`，於 headless session 內以 `--allowedTools 'Bash(claude --version)'` 執行，stdout `result` 為 `2.1.277 (Claude Code)`。受測環境：2026-09-20，執行身分為本機安裝 Claude Code 的 OS 使用者；受測對象是**本機 CLI 環境**，與 repo 無關——換機器須重驗。本格原記 2026-09-11 的 `2.1.267`，#112 發現漂移，依 `R10` 由 #114 重驗更新） |
