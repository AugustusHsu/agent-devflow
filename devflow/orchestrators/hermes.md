# orchestrator: hermes

衍生值對照表。分節與狀態欄依 `R9`：**通用**節的值欄宣稱可重用的工具能力或程序（repo 與環境只是可替換的參數），**本機**節的值欄宣稱具名的環境 instance（本機安裝的工具、帳號或組織、本 repo 的設定與資源）；狀態取 `✅`／`📝`／`⬜` 三值之一，兩節的 `✅` 判準各依 `R9` 定義，非 `✅` 不得在流程中當作可用。「職位」欄記該格是哪個職位的用法（詞彙見 `seats/`），`—` 表示是工具或資源本身的性質，不專屬任一職位。

## 通用

| 面向 | 職位 | 值 | 狀態 |
|---|---|---|---|
| 派工（`L2`） | coordinator | 用 `terminal` 直接啟動 coder CLI（見 `coders/`），保存程序或 session 識別。本派工路徑**不使用 `delegate_task`** | ✅ 可用（實測 2026-09-11，驗證方式：在 orchestrator 的頂層 session 以 `terminal(command="cd <worktree> && systemd-run --user --scope --unit=coder-<N> --collect -q <coders/ 表上的 CLI 指令> > <stdout 檔> 2> <stderr 檔> < /dev/null", background=true)` 啟動 coder（`systemd-run --scope` 把 coder 及其所有子孫放進專用 cgroup，`--unit` 名稱是中斷交接格的中斷把手；無 systemd 的平台另找監督邊界），記下該工具回傳的 pid；再於同一台機器讀回三項——(1) `pstree -p <該 pid> \| head -1` 的鏈上出現 `claude(<coder pid>)`；(2) `tr '\0' ' ' < /proc/<coder pid>/cmdline` 以 `claude -p` 開頭；(3) `readlink /proc/<coder pid>/cwd` 等於該 worktree 路徑。第 (1) 項連同完整的 `terminal` 呼叫紀錄，證明本次 coder 是由 `terminal` 啟動的外部進程；「不用 `delegate_task`」由呼叫紀錄判定（紀錄中無 `delegate_task` 呼叫），本格**不宣稱** `delegate_task` 的進程樹長什麼樣——那未實測。值欄「保存程序或 session 識別」兩項分別由 `terminal(background=true)` 回傳、稍後仍可用來讀回該進程的 pid，與 stdout 檔內 JSON 的 `session_id`（見 `coders/claude-code.md` 的 headless 執行格）判定。**不要用 `ps -eo pid,args \| grep 'claude -p'`**：coder 的 prompt 為多行時 `ps` 的 args 顯示不可靠，會讀到空結果而誤判進程不存在。此程序不依賴本次的 prompt 或 repo，第三者現在可獨立重跑。受測環境：2026-09-11；orchestrator 為 Hermes Agent v0.21.1（2026.9.7），coder 為 Claude Code 2.1.267 headless；程序讀回依賴 Linux 的 `/proc` 與 `pstree`（無 `/proc` 的平台須另找讀回方式）；執行身分為本機 OS 使用者，與 coder 進程同 UID；`readlink /proc/<pid>/cwd` 受 `PTRACE_MODE_READ_FSCREDS` 檢查（`proc_pid_cwd(5)`；同 UID 但目標 `PR_SET_DUMPABLE=0` 時讀取失敗），`/proc/<pid>/cmdline` 不受該檢查；本次在同 UID、預設 dumpable、無額外 LSM 限制的環境兩者皆讀取成功，其他環境（容器、不同 UID、Yama ptrace_scope）須自行確認；pstree (PSmisc) 23.7；systemd 255（255.4-1ubuntu8.17）user session、cgroup v2（`systemd-run --scope` 所需）；目標 repo `AugustusHsu/agent-devflow`；證據：issue #33 留言 https://github.com/AugustusHsu/agent-devflow/issues/33#issuecomment-5624350935（完整 `terminal` 呼叫）＋ https://github.com/AugustusHsu/agent-devflow/issues/33#issuecomment-5624355903（進程樹與 `/proc` 讀回）） |
| 建 worktree（`I2`） | coordinator | `git worktree add ../<repo>.worktrees/<N> <N>-<slug>` | ✅ 可用（實測 2026-09-11，驗證方式同 `coders/claude-code.md` 的 worktree 格，主體換成 orchestrator：orchestrator 在主 checkout 以 `terminal` 執行 `git branch <N>-<slug> origin/main`，再執行 `git worktree add ../<repo>.worktrees/<N> <N>-<slug>`；啟動 coder 前以 `git worktree list` 確認輸出含該路徑與分支名，並以 `git -C ../<repo>.worktrees/<N> rev-parse HEAD` 確認該 worktree 的 HEAD 等於 base commit。判定點：worktree 路徑落在 repo 目錄之外（`I2`），主 checkout 那一列仍停在原分支與原 commit，且 coder 只收到該路徑。此程序不依賴本次的 issue 或 repo，第三者現在可獨立重跑。受測環境：2026-09-11；orchestrator 為 Hermes Agent v0.21.1（2026.9.7），以 `terminal` 工具執行本機 `git version 2.43.0` 的 `branch`／`worktree` 子命令；執行身分為對該 repo 工作目錄具寫入權限的本機 OS 使用者（本步驟不需 forge 權限）；目標 repo `AugustusHsu/agent-devflow`，base `origin/main @ 333d087`；證據：issue #33 留言 https://github.com/AugustusHsu/agent-devflow/issues/33#issuecomment-5624350935） |
| HITL（`L3`） | coordinator | 向人提問，答案寫回 issue 留言（持久紀錄）；派工前未決事項為空 | ⬜ 未測（值欄已依 `R7` 改寫：原文「`clarify` 問人」把工具名當機制，`L3` 明文「提問通道只是通道」。迴路「coder BLOCKED 留言 → 人類裁決留言（含依據）→ 重派」在 issue #4 確實走過（留言 5622863707 → 5623110580 → 5623153788），但那是**歷史紀錄**——第三者現在只能讀回、不能重新執行「提問 → 裁決 → 寫回 → 重派」，依 `R8` 屬追溯讀法，不足以 `✅`。升格需要第三者現在可主動重跑的演練：派工前 issue「未決事項」為空（`L1`），coder 執行中在 issue 留言提出未決事項並停下，orchestrator 向人提問、把裁決連同依據寫回同一 issue、再以 `--resume` 重派——本格判定的是 orchestrator 側這三步是否發生；coder 是否**自發**停下屬 `coders/claude-code.md` 的「HITL（`L3` 遵從）」格，可共用同一場演練但分別判定） |
| 平行上限 | coordinator | 上限依 `stage`：stage 1–2 由 `ST` 定（`ST1`：同時只有一個任務），沒有可調的旋鈕；stage 3 起才由人設定，並依 `P1` 逐項確認寫進 issue（`P4`） | ⬜ 未測（本單依 `R7` 改寫值欄，狀態不升。理由：原值欄「人設」在今日的 stage 1 不是實際生效的機制——`devflow.yml` 為 `stage: 1`，`ST1` 已把同時活躍任務定為 1，沒有人可設定的上限；`P1`／`P4` 住第 12 節，依 `ST3` 要 stage 3 才生效。本 issue 雖已填「平行前提」四欄（write scope、阻塞依賴、共用契約、外部資源），但那是 stage < 3 的預先累積：只有一張活躍任務時，`P1` 的判定（write scope 不重疊、共用契約無人同時改）無從被檢驗，依 `R8`（未執行的推測不構成 `✅`，且不得由「行為符合規則」反推機制已生效）不足以升格。待 stage 3 首次同時派兩張任務、由人實際設定上限並逐項確認 `P1` 時再測） |
| 流程指令住哪 | coordinator | `devflow/orchestrators/hermes/SKILL.md`（`name: devflow-orchestrator`、`version: 0.1.0`；stage ≥ 1 的單一任務生命週期固定列為 1→10）是**規範上**的流程指令住處。但 Hermes 只掃描它的 skill 發現路徑，**權威來源是程式碼**：`agent/skill_utils.py` 的 `get_all_skills_dirs()`（local dir、`skills.create_dir`、`skills.external_dirs`）加 `get_project_skills_dirs()`（已 trust 的 `<project-root>/.hermes/skills/`、`<project-root>/.agents/skills/`）；文件對應段在 `website/docs/user-guide/features/skills.md`「Project-local skills」與「create_dir」節（Hermes 0.21.2 為第 417–465 行）。本格不重述完整清單——路徑集合隨版本變，引用前看該版程式碼。`devflow/orchestrators/hermes/` 本身不在上述任何發現路徑內，要被讀就得把它放進（或 symlink 進）其中一條路徑，再依該路徑的規則啟用。採用 agent-devflow 的 repo 走 project-local 這條：repo 內一條受版控的 symlink `<project-root>/.hermes/skills/devflow-orchestrator → ../../devflow/orchestrators/hermes`（相對路徑，git 以 mode `120000` 追蹤，跨 clone 有效）把該目錄接進發現路徑，採用者在自己的 clone 內跑一次 `hermes skills trust` 後載入；不必搬檔，也不必在安裝腳本多一步。只放這一條，**不加 `.agents/skills/`**——兩個 project-local 位置都放會重複索引同一份檔。未 trust 時不會載入；**據文件推導**（非原文）此時會有提示——文件記載的 banner 只針對 project-local 位置發現的未信任 skill。 | ✅ 可用（第三者現在照本格所附的驗證方式做，能得到值欄宣稱的結果——兩條路徑都已由本格作者以外的人重跑過，見下面「第三者重跑」。驗證方式（兩條，擇一或並用）：(1) 在已 trust 的 clone 內新開一個 Hermes session，請它原文回報 `skills_list`，應含 `name: devflow-orchestrator`、`version: 0.1.0`；(2) 不開 session 的直接探針——`python3 -c` 匯入 `agent/skill_utils.py`，對該 repo 根呼叫 `get_project_skills_dirs()` 取候選目錄，再以 `iter_skill_index_files()` 取索引檔，應得 `<project-root>/.hermes/skills/devflow-orchestrator/SKILL.md`，其 `realpath` 落在 `<project-root>/devflow/orchestrators/hermes/SKILL.md`。**探針陷阱**：`find_project_root()` 以環境變數 `TERMINAL_CWD` 優先於 `Path.cwd()`（`skill_utils.py:428`），從 Hermes 的 `terminal` 工具跑探針會被 orchestrator 自己的 cwd 蓋掉而回 `[]`；要 `env -u TERMINAL_CWD` 或明傳 `start=`。證據：issue #119 的 spike（orchestrator 2026-09-20 於拋棄式 clone `/tmp/spike-pl` 執行探針）——`project root: /tmp/spike-pl \| trusted: True`、`candidate dirs: [/tmp/spike-pl/.hermes/skills]`、`index files via symlink: [/tmp/spike-pl/.hermes/skills/devflow-orchestrator/SKILL.md]`、`name: devflow-orchestrator \| version: 0.1.0 \| realpath: /tmp/spike-pl/devflow/orchestrators/hermes/SKILL.md`；同一支探針把 symlink 換成實體複本當對照組，索引結果相同，故「跟隨 repo 內的 symlink」不是複本造成的假象。機制依據：`iter_skill_index_files()` 用 `os.walk(followlinks=True)`（`agent/skill_utils.py:745`）。本 repo 這端由實作位驗到檔案系統與版控狀態：`ls -l .hermes/skills/` 顯示 `devflow-orchestrator -> ../../devflow/orchestrators/hermes`、`test -f .hermes/skills/devflow-orchestrator/SKILL.md` 通過、`git ls-files -s .hermes/` 回 mode `120000`。**第三者重跑**：(i) PR #123 的 fresh-context 審查者（`R1`、`R2`）依本格驗證方式 (2) 在自己的拋棄式 clone `/tmp/rv123` 重跑同一支探針並成功——回報 `root /tmp/rv123`、`/tmp/rv123/.hermes/skills/devflow-orchestrator/SKILL.md name: devflow-orchestrator version: 0.1.0`，並自行核到本機 `iter_skill_index_files()` 第 745 行確為 `os.walk(..., followlinks=True)`；(ii) 本 repo 這端走的是驗證方式 (1)——issue #129 由 orchestrator 在已 trust 的本機 clone **新開** Hermes session，`skills_list` 讀回 `name: devflow-orchestrator`／`version: 0.1.0`（那一側的受測環境、probe 指令與負結果對照記在本機節「skill 接入」列）。兩條都不依賴 #119 那次執行的一次性條件（`R8`）。受測環境（`R10`）——**首次驗證**：2026-09-20，Hermes 0.21.2（`skill_utils.py` 的 `:428`、`:745` 兩個行號依該版）；spike 由 orchestrator（Hermes）在拋棄式 clone `/tmp/spike-pl` 執行，symlink 與版控狀態由實作位 Claude Code 2.1.277 headless 在 worktree `agent-devflow.worktrees/119` 執行、git 2.43.0；目標 repo `AugustusHsu/agent-devflow` @ `9cc8634`。**最近確認**：2026-09-21——2026-09-20 由 PR #123 審查者以 Hermes 0.21.2 在 `/tmp/rv123` 重跑探針，2026-09-21 由 issue #129 以 Hermes `v0.21.3 (2026.9.14)` 在本機 clone `/home/augustushsu/code/Python/agent-devflow` 新開 session 讀回，兩次結果都與值欄相符。共同條件：執行身分為本機使用者；驗證方式 (2) 只讀該 clone 與 `agent/skill_utils.py`，驗證方式 (1) 另需該 clone 已 `hermes skills trust`，且 project-local 發現的起點是 `resolve_agent_cwd()`（session override → `TERMINAL_CWD` → `os.getcwd()`）——從 Hermes 的 terminal 工具或 gateway 子程序啟動會繼承 `_HERMES_GATEWAY`／`TERMINAL_CWD` 而蓋掉 shell 的 cwd，該情形要加 `--in <repo>`；乾淨終端在 repo 內不必加（見本機節「skill 接入」列）。Hermes 換版本身不改本格狀態，但發現路徑集合與 `followlinks` 都隨版本變，引用前先比對自己的版本、有差異自己重驗。最近確認的 forge 紀錄：https://github.com/AugustusHsu/agent-devflow/pull/123#pullrequestreview-5260420104 、https://github.com/AugustusHsu/agent-devflow/issues/129 。接入前本機是什麼狀態、用哪三條指令查的，記在本機節那列，不在此重述（`I5`）。證據：issue #119、#118） |
| skill 啟用邊界（`G3`） | coordinator | skill 指向已核准版本，不指向編輯中的候選；驗證「載到正確內容與版本」，不只驗 symlink 存在 | ⬜ 未測（`G3` 說新版的啟用點是合入後的下一個 session 邊界，**前提是該 skill 真的被載入**——接入之前本機這套 Hermes 未含 repo 的 `devflow/orchestrators/hermes/SKILL.md`（2026-09-20 的三條查證指令與結果見本機節的「skill 接入」列），沒有接入就沒有啟用邊界可驗；接入已於 `bee4377` 完成，四步驗證待下一次 SKILL.md 改版時執行。驗證設計（須含 `G3` 的「合入」條件，不是只測 session 重載）：(1) 舊 session 先載入**已核准、已合入 main** 的舊版，請 Hermes 原文回報 `name`／`version`；(2) 新版走完整流程——開 issue、PR、審查、**合入 main**（或在隔離測試 repo 模擬同一套「已核准指標切換」）；(3) 合入後，舊 session 再問同一句——應仍回報舊版（進行中任務續用舊 G）；(4) 合入後**新開**的 session 問同一句——應回報新版。四步都成立才算「啟用點是合入後的下一個 session 邊界」。**不可**在進行中的 session 直接改工作樹的 SKILL.md 來測——那會讓新 session 指向未核准的候選，違反本格值欄前半。同時要驗值欄後半：請 Hermes 回報它載到的 skill `name` 與 `version`，須逐字等於 repo 檔的第 2、4 行，僅確認 symlink 存在不算。此設計也可當「已知事實」第 23 行（改磁碟檔不會替換已載入的規則）的實測載體，但那條本單不動） |
| 中斷交接 | coordinator | 保留 issue、分支、worktree、HEAD、未提交內容、最後可信驗證；在本格所載 `--allowedTools` 限制下，確認 coder 專用 cgroup 內所有成員已停才重派；白名單放寬到可建立或遷移 unit 時不適用 | ✅ 可用（實測 2026-09-11，驗證方式：在 coder 執行途中由 orchestrator 中斷（`kill` 該 coder 進程），中斷當下把值欄的六項保留物逐一讀回並貼成 issue 留言——(1) `gh issue view <N> --json state` 為 `OPEN`；(2) `git branch --list <N>-<slug>` 有該分支；(3) `git worktree list` 仍含該 worktree 路徑與分支；(4) `git -C <worktree> rev-parse HEAD` 的 sha；(5) `git -C <worktree> status --porcelain` 的未提交／未追蹤清單；(6) 中斷前最後一次驗證指令的原始輸出。再確認舊 coder 已停——coder 啟動時已放進專用 cgroup（見派工格的 `systemd-run --user --scope --unit=coder-<N>`），中斷用 `systemctl --user kill --signal=TERM coder-<N>.scope`（核心對 cgroup 當下全部成員送訊號），**中斷前**先確認 scope 確曾承載 coder：`systemctl --user is-active coder-<N>.scope` 回 `active` 且 `$CG/cgroup.procs` 含 coder pid（`CG=$(find /sys/fs/cgroup/user.slice -type d -name 'coder-<N>.scope')`）——從未存在的 unit 也會回 `inactive`，沒有這步的 `inactive` 不算證據。已停的判定是中斷後 `is-active` 回 `inactive`、或 `$CG/cgroup.events` 的 `populated 0`。**本格判定的範圍是「coder cgroup 內的所有成員已停」**：scope 內的程序若自己執行 `systemd-run --user --scope` 可遷到 sibling cgroup 逃出（審查中實證），cgroup 不納管已遷出者。因此受測條件之一是 coder 的 `--allowedTools` 白名單**不含** `systemd-run`／`systemctl`／`busctl` 等可建立或遷移 unit 的指令（本次白名單：`Read,Edit,Write,Bash(git add *),Bash(git commit *),Bash(git diff *),Bash(git log *),Bash(git status *),Bash(git show *),Bash(git rev-parse *),Bash(gh issue view *),Bash(gh api *),Bash(grep *),Bash(sed -n *),Bash(cat *),Bash(nl *)`）；白名單放寬到能執行任意指令時，本格不適用，須另納管 coder 建立的 unit。cgroup 成員資格由核心維護、fork／`setsid` 雙 fork 出的子孫自動繼承，不需要 PID 快照，沒有「快照後 fork」或「reparent 後 `pgrep -P` 漏看」的 TOCTOU 缺口（三者皆在審查中以反例實證：PID 快照＋逐一 `kill -0` 不足，cgroup 封住前兩者）——確認後才以 `--resume <session_id>` 重派，重派 prompt 告知「你被中斷過，先讀工作區狀態」。判定點三項皆須成立：重派後的 coder 讀到的 HEAD 與未提交內容和 (4)(5) 逐字相同；同一 worktree 沒有第二個 coder 進程；重派 stdout 的 `session_id` 與被中斷那段相同（見 `coders/claude-code.md` 的 `--resume` 交接格）。此程序不依賴本次的 issue 或 prompt，第三者現在可對任一 headless coder 重跑。受測環境：2026-09-11；orchestrator 為 Hermes Agent v0.21.1（2026.9.7），coder 為 Claude Code 2.1.267 headless（`--resume` 需被中斷段的 `session_id`；被 SIGTERM 的 headless session **不會**輸出 JSON，須從 `~/.claude/projects/<cwd 路徑編碼>/` 取最新 `.jsonl` 檔名，首行 `sessionId` 即是），git 2.43.0，gh 2.97.0；監督邊界為 systemd 255（255.4-1ubuntu8.17）user session 的 transient scope，cgroup v2；不需 root）；執行身分為本機 OS 使用者，須與 coder 進程同一使用者（`kill` 需要），且重派須由**同一 OS 使用者**執行（`--resume` 讀該使用者 `~/.claude/` 下的 session 紀錄，換身分即無法續接），並具 `AugustusHsu/agent-devflow` 的 `gh` 留言權限；目標 repo `AugustusHsu/agent-devflow`；證據：issue #33 cgroup 已停判定重測 https://github.com/AugustusHsu/agent-devflow/issues/33#issuecomment-5624794146 ＋ 中斷時狀態留言 https://github.com/AugustusHsu/agent-devflow/issues/33#issuecomment-5624437437 ＋ https://github.com/AugustusHsu/agent-devflow/issues/33#issuecomment-5624479141） |

## 本機

| 面向 | 職位 | 值 | 狀態 |
|---|---|---|---|
| skill 接入 | coordinator | 本 repo 的 `devflow/orchestrators/hermes/SKILL.md` 自 `bee4377` 起，經受版控的 symlink `.hermes/skills/devflow-orchestrator → ../../devflow/orchestrators/hermes` 接進 Hermes 的 project-local 發現路徑；本機 `~/.hermes/config.yaml` 的 `trusted_project_dirs` 含 `~/code/Python/agent-devflow`，本機這套 Hermes 因此在本 repo 載入 `devflow-orchestrator` 0.1.0（trust 是 host 端設定、不隨 repo 走，由 orchestrator 在合併後執行——coder 不動 `~/.hermes/`） | ✅ 可用（值欄三項全部成立（`R7`）：(a) 起算的 sha `bee4377` 已回填（與「skill 啟用邊界（`G3`）」列的是同一個 commit）；(b) `trusted_project_dirs` 已由 orchestrator 在合併後於本 repo 跑 `hermes skills trust` 寫入，含 `~/code/Python/agent-devflow`；(c)「本機這套 Hermes 在本 repo 載入 `devflow-orchestrator` 0.1.0」已由 orchestrator 在新開的 Hermes session 讀回。**驗證方式**（第三者在本機的當下環境現在能執行）：寫一支 probe 檔，要求新 session 呼叫 `skills_list` 並以固定格式回報，跑 `hermes chat --oneshot --query-file <probe> --reasoning low -t skills --in /home/augustushsu/code/Python/agent-devflow`；回報應為 `SKILL_LOADED: yes`、`VERSION: 0.1.0`、`FIRST_H2: 一、觸發`、`COUNT: 61`、`CWD: /home/augustushsu/code/Python/agent-devflow`。**對照組（負結果）**：同一支 probe、去掉 `--in`、**從 Hermes gateway 的 terminal 工具啟動**（shell 的 cwd 已在本 repo 內）——得 `SKILL_LOADED: no`、`COUNT: 60`、`CWD: /home/augustushsu`；故正結果不是「反正都會載入」的假象。**決定 cwd 的是啟動環境，不是 config 的 `terminal.cwd`**：project-local 發現的起點是 `agent/skill_utils.py::find_project_root`，它呼叫 `resolve_agent_cwd()`，實際順序是 session override（`_SESSION_CWD`）→ scope 的 `TERMINAL_CWD` → `os.getcwd()`。從 Hermes 的 terminal 工具或 gateway 子程序啟動的 `hermes chat` 會繼承 `_HERMES_GATEWAY=1` 與 `TERMINAL_CWD`；有 gateway marker 時 placeholder `terminal.cwd: .` 走 `gateway/cwd_placeholder.py::resolve_placeholder_terminal_cwd()`（取繼承的 `TERMINAL_CWD`，沒有則 home fallback），**不是** `os.getcwd()`，shell 的 cwd 因此被蓋掉。獨立終端（無 marker）則由 `cli.py::_mirror_config_to_env()` 把 placeholder `.` 換成 `os.getcwd()`，shell 的 cwd 生效。受控矩陣（同一支 probe、同一 repo，2026-09-21 實跑四組）：gateway 衍生（`_HERMES_GATEWAY=1` ＋ `TERMINAL_CWD=$HOME`）＋無 `--in` → `SKILL_LOADED: no`、`CWD: /home/augustushsu`；gateway 衍生＋有 `--in` → `yes`、`COUNT: 61`、`CWD: <repo>`；乾淨環境（`env -u _HERMES_GATEWAY -u TERMINAL_CWD`）、shell cwd 在 repo、無 `--in` → `yes`、`COUNT: 61`、`CWD: <repo>`；只留 `_HERMES_GATEWAY=1`（`env -u TERMINAL_CWD`）、shell cwd 在 repo、無 `--in` → `no`、`CWD: /home/augustushsu`——第四組證明 marker 本身就足以改變結果，不需要繼承 `TERMINAL_CWD`。故 `--in <repo>` 在兩種啟動環境皆有效，但必要性依情境：**gateway 衍生環境下必要**（無 `--in` 即落到 `$HOME`、不載入）；**乾淨終端且 shell cwd 已在 repo 內時不必要**。Telegram／gateway session 的 cwd 由該 session 決定，本格不宣稱。受測環境（`R10`）——**首次驗證＝最近確認**：2026-09-21，Hermes `v0.21.3 (2026.9.14)`（`hermes --version`），由 orchestrator（Hermes）自跑，模型 reasoning low；執行身分為本機使用者，讀得到 host 端 `~/.hermes/`（trust 是 host 端設定、不隨 repo 走，coder 不動該目錄）；host 條件：`~/.hermes/config.yaml` 的 `skills.trusted_project_dirs` 含 `~/code/Python/agent-devflow`——trust 被移除使該項不再成立時，本格依 `R10` 降為 `📝`（`terminal.cwd` 為 placeholder `.`，但它只是被解析的對象，不是結果的因）；**會改變結果的條件——啟動環境**：程序有無 `_HERMES_GATEWAY` 與繼承來的 `TERMINAL_CWD`（從 Hermes 的 terminal 工具或 gateway 子程序啟動即帶；placeholder 解析時 `MESSAGING_CWD` 與 home fallback 也可能決定取到的值）。上列正／負兩組都在帶 marker 的環境跑，差別只在 `--in`；乾淨終端（無 marker）在 repo 內不加 `--in` 亦得正結果。目標環境是本機 checkout `/home/augustushsu/code/Python/agent-devflow`（`AugustusHsu/agent-devflow`，含 `bee4377` 之後）。Hermes 換版本身不改本格狀態，但發現路徑集合隨版本變，引用前先比對自己的版本。最近確認的 forge 紀錄：https://github.com/AugustusHsu/agent-devflow/issues/129 。**接入前的起點**（2026-09-20，由實作位於 worktree `agent-devflow.worktrees/116` 自跑；當時本機所有發現路徑皆未含此檔——`~/.hermes/skills/` 無 symlink 亦無複本、`config.yaml` 無 `create_dir` 亦無 `external_dirs`、本 repo 兩個 project-local 位置無檔）：(1) `find ~/.hermes/skills -type l` → **無輸出**，無 symlink；(2) `find ~/.hermes/skills -name SKILL.md -exec grep -l devflow-orchestrator {} \;` → **無輸出**，無複製——該 session 的 coder 權限層擋下帶 `-exec` 的 `find`，改跑等價的 `grep -rl devflow-orchestrator --include=SKILL.md ~/.hermes/skills`，同樣無輸出；同一棵樹 `find ~/.hermes/skills -name SKILL.md \| wc -l` 回 `64`，且把 pattern 換成已知存在的 `agent-dev-workflow` 重跑回 2 個命中（`autonomous-ai-agents/agent-dev-workflow`、`autonomous-ai-agents/coding-agent-orchestration`），故此負結果不是空掃；(3) `grep -n external_dirs ~/.hermes/config.yaml` → **無輸出**（該檔不在該 session 允許目錄內、被同一權限層擋下，改以 Read 全檔核對：`skills:` 節在第 142–143 行、只有 `creation_nudge_interval: 15`，全檔 312 行無 `external_dirs`）。當時實際載入的是 `~/.hermes/skills/autonomous-ai-agents/agent-dev-workflow/SKILL.md`（front matter 第 2、4 行為 `name: agent-dev-workflow`／`version: 1.0.0`），與 repo 那份（`name: devflow-orchestrator`／`version: 0.1.0`）不同檔、不同名、不同版本；「本 Hermes 這週（#98～#114）實際依循的是 `agent-dev-workflow` 1.0.0 加 `coding-agent-orchestration`」是 orchestrator（Hermes）自報，實作位無法從 worktree 獨立讀回。接入前查證的受測環境：2026-09-20；由實作位 Claude Code 2.1.277 headless 執行，需 `--add-dir /home/augustushsu/.hermes/skills` 才放行（工作目錄範圍是疊在 `--allowedTools` 之上的另一層，見 `coders/claude-code.md` 的「權限：`--allowedTools`」格）；本機 `find` 實作為 `bfs 4.1.1`、`grep` 實作為 `ugrep 7.8.4`（非 GNU findutils／GNU grep）；受測對象是本機 `~/.hermes/` 安裝（`config.yaml` 的 `_config_version: 42`），Hermes 版本未由實作位讀回（`hermes --version` 不在該 session 白名單）；目標 repo `AugustusHsu/agent-devflow` @ `e305f28`。本次接入這端，實作位在 worktree `agent-devflow.worktrees/119` 建了 symlink 並驗到 git mode `120000`（指令與結果見通用節「流程指令住哪」列）——那驗的是檔案系統與版控狀態，不是 Hermes 載入。證據：issue #119、#118、#116） |

## 已知事實（文件推導，待實測）

- Hermes 的專案 context 在 session 開始載入；改磁碟檔不會替換已載入的規則（`G3` 的依據）。
- Hermes 子代理不能 `clarify`；提問只能由頂層 session 發出。
